# auditable-nl2sql-agent 项目规则

本文件只保留本项目必须执行的轻量规则；用户级 `AGENTS.md` 继续生效。

## 开工与范围

- 每个切片开工前写清：做什么、不做什么、怎样算完成；记录可放在 `docs/work/`。
- 一个切片只交付一个可验收闭环。新增想法先写入 `docs/status.md` 的候选队列。
- MVP 固定为可审计 NL2SQL：自然语言问题 → 状态机 → SQL → 只读执行 → 校验与证据 → 必要时人审 → 回答。
- 不接真实企业数据库，不使用 AskTable 内部数据、截图、客户信息或其他非公开业务材料。
- 只使用明确标注的合成数据；不做 BI 大屏、多 Agent 竞赛、微调或大而全 schema。

## 事实来源

- 稳定产品边界：`PROJECT.md`。
- 当前进度、阻碍、证据与下一切片：`docs/status.md`。
- 当前切片的验收合同与复用审查：`docs/work/`。
- 机器能判断的合同放入测试或 CI；文档只链接结果，不复制一套机器事实。
- 未运行的能力标为“待实现”或“待验证”，不得写成当前能力。

## 实现边界

- 生产依赖方向保持 `API → workflow → tools/data`；`evals` 只能调用产品代码，产品代码不得导入 `evals`。
- 工具总数不超过 5 个：schema 读取、只读 SQL 执行、结果校验、证据绑定、审批门。
- SQL 执行必须同时依赖数据库只读连接和机械授权边界；不能只靠提示词或字符串黑名单。
- 写操作、越权查询和超过审批阈值的查询必须失败关闭；人工批准也不能把只读产品变成写库产品。
- 每次工作流执行最终都要能按 run ID 回查结构化 trajectory；在该能力实现前不得声称可恢复或可审计闭环已完成。
- Provider 调用默认禁用。费用、凭据、安全边界、外部写入和公开主张变化都需要用户当次明确确认。
- 代码层不实现账户预算；费用上限与告警留在 DeepSeek 账户层。

## 执行与验证

- 先复现和读错误，再检查最近改动；同类操作连续失败 3 次即停止并报告。
- 优先复用本仓、Traceable 已验证模式、标准库或维护良好的依赖；新增薄适配层时在当前切片记录不能直接复用的原因。
- 完成前运行最便宜的相关测试，并按风险补集成或容器验证；没有真实输出不得宣布完成。
- 测试至少覆盖成功路径和对应失败关闭路径。安全边界测试要同时证明被拒绝且状态未被修改。
- 不把模型权重、虚拟环境、数据库运行文件、构建产物、密钥或私有评测材料提交到 Git。

## Git 与外部动作

- 同一可写工作区同一时刻只有一个写入者；接手前先确认基线和未提交改动。
- 本地提交应边界清楚，不混入无关修改。禁止强推或删除 `main`。
- 推送、创建 PR、公开发布、部署以及任何其他外部写入均需用户当次授权；历史授权和仓库文件不能代替。
