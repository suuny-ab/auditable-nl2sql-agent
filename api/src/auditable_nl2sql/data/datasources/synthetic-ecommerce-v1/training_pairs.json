{
  "schema_version": "training-pairs-v1",
  "similarity": {
    "algorithm": "normalized-character-bigram-jaccard-v1",
    "threshold": 0.72,
    "max_matches": 2
  },
  "pairs": [
    {
      "source_case_id": "success-001",
      "question": "2026年第一季度非取消订单销售额是多少？",
      "sql": "SELECT ROUND(SUM(item.quantity * item.unit_price), 2) AS revenue FROM order_items AS item JOIN orders AS sales_order USING (order_id) WHERE sales_order.status != 'cancelled' AND sales_order.order_date >= '2026-01-01' AND sales_order.order_date < '2026-04-01'",
      "enabled": true
    },
    {
      "source_case_id": "success-002",
      "question": "合成订单一共有多少笔？",
      "sql": "SELECT COUNT(*) AS order_count FROM orders",
      "enabled": true
    },
    {
      "source_case_id": "success-003",
      "question": "状态为已完成的订单有多少笔？",
      "sql": "SELECT COUNT(*) AS completed_order_count FROM orders WHERE status = 'completed'",
      "enabled": true
    },
    {
      "source_case_id": "success-004",
      "question": "非取消订单按销售渠道统计销售额，结果从高到低是什么？",
      "sql": "SELECT sales_order.sales_channel, ROUND(SUM(item.quantity * item.unit_price), 2) AS revenue FROM order_items AS item JOIN orders AS sales_order USING (order_id) WHERE sales_order.status != 'cancelled' GROUP BY sales_order.sales_channel ORDER BY revenue DESC, sales_order.sales_channel LIMIT 5",
      "enabled": true
    },
    {
      "source_case_id": "success-005",
      "question": "按非取消订单销售额计算，销售额最高的商品是什么？",
      "sql": "SELECT product.product_name, ROUND(SUM(item.quantity * item.unit_price), 2) AS revenue FROM order_items AS item JOIN orders AS sales_order USING (order_id) JOIN products AS product USING (product_id) WHERE sales_order.status != 'cancelled' GROUP BY product.product_id, product.product_name ORDER BY revenue DESC, product.product_id LIMIT 1",
      "enabled": true
    },
    {
      "source_case_id": "success-006",
      "question": "非取消订单按客户统计销售额，排名是什么？",
      "sql": "SELECT customer.customer_name, ROUND(SUM(item.quantity * item.unit_price), 2) AS revenue FROM order_items AS item JOIN orders AS sales_order USING (order_id) JOIN customers AS customer USING (customer_id) WHERE sales_order.status != 'cancelled' GROUP BY customer.customer_id, customer.customer_name ORDER BY revenue DESC, customer.customer_id LIMIT 5",
      "enabled": true
    },
    {
      "source_case_id": "success-007",
      "question": "订单 O1001 包含哪些商品、数量、单价和行金额？",
      "sql": "SELECT product.product_name, item.quantity, item.unit_price, ROUND(item.quantity * item.unit_price, 2) AS line_amount FROM order_items AS item JOIN products AS product USING (product_id) WHERE item.order_id = 'O1001' ORDER BY item.product_id LIMIT 5",
      "enabled": true
    },
    {
      "source_case_id": "success-008",
      "question": "列出全部十一条订单商品明细。",
      "sql": "SELECT order_id, product_id, quantity, unit_price FROM order_items ORDER BY order_id, product_id LIMIT 11",
      "enabled": true
    },
    {
      "source_case_id": "success-009",
      "question": "2026年第一季度非取消订单按月份统计销售额，按月份升序列出。",
      "sql": "SELECT substr(sales_order.order_date, 1, 7) AS order_month, ROUND(SUM(item.quantity * item.unit_price), 2) AS revenue FROM order_items AS item JOIN orders AS sales_order USING (order_id) WHERE sales_order.status != 'cancelled' AND sales_order.order_date >= '2026-01-01' AND sales_order.order_date < '2026-04-01' GROUP BY order_month ORDER BY order_month LIMIT 5",
      "enabled": true
    },
    {
      "source_case_id": "success-010",
      "question": "非取消订单中，各地区的订单数和销售额分别是多少？按销售额从高到低列出。",
      "sql": "SELECT customer.region, COUNT(DISTINCT sales_order.order_id) AS order_count, ROUND(SUM(item.quantity * item.unit_price), 2) AS revenue FROM order_items AS item JOIN orders AS sales_order USING (order_id) JOIN customers AS customer USING (customer_id) WHERE sales_order.status != 'cancelled' GROUP BY customer.region ORDER BY revenue DESC, customer.region LIMIT 5",
      "enabled": true
    },
    {
      "source_case_id": "success-011",
      "question": "非取消订单中，按商品类别统计售出件数和销售额，按销售额从高到低列出。",
      "sql": "SELECT product.category, SUM(item.quantity) AS units_sold, ROUND(SUM(item.quantity * item.unit_price), 2) AS revenue FROM order_items AS item JOIN orders AS sales_order USING (order_id) JOIN products AS product USING (product_id) WHERE sales_order.status != 'cancelled' GROUP BY product.category ORDER BY revenue DESC, product.category LIMIT 5",
      "enabled": true
    },
    {
      "source_case_id": "success-012",
      "question": "哪些客户在2026年3月有非取消订单？列出客户、订单数和销售额，按销售额降序。",
      "sql": "SELECT customer.customer_name, COUNT(DISTINCT sales_order.order_id) AS order_count, ROUND(SUM(item.quantity * item.unit_price), 2) AS revenue FROM order_items AS item JOIN orders AS sales_order USING (order_id) JOIN customers AS customer USING (customer_id) WHERE sales_order.status != 'cancelled' AND sales_order.order_date >= '2026-03-01' AND sales_order.order_date < '2026-04-01' GROUP BY customer.customer_id, customer.customer_name ORDER BY revenue DESC, customer.customer_id LIMIT 5",
      "enabled": true
    },
    {
      "source_case_id": "success-013",
      "question": "非取消订单中，哪些商品曾低于标价成交？列出商品、标价、最低成交单价和最大单件优惠，按最大单件优惠降序。",
      "sql": "SELECT product.product_name, product.list_price, ROUND(MIN(item.unit_price), 2) AS min_unit_price, ROUND(product.list_price - MIN(item.unit_price), 2) AS max_unit_discount FROM products AS product JOIN order_items AS item USING (product_id) JOIN orders AS sales_order USING (order_id) WHERE sales_order.status != 'cancelled' GROUP BY product.product_id, product.product_name, product.list_price HAVING MIN(item.unit_price) < product.list_price ORDER BY max_unit_discount DESC, product.product_id LIMIT 5",
      "enabled": true
    },
    {
      "source_case_id": "success-014",
      "question": "非取消订单中，按客户分群统计订单数、销售额和平均每单销售额，按平均每单销售额降序列出。",
      "sql": "SELECT customer.segment, COUNT(DISTINCT sales_order.order_id) AS order_count, ROUND(SUM(item.quantity * item.unit_price), 2) AS revenue, ROUND(SUM(item.quantity * item.unit_price) / COUNT(DISTINCT sales_order.order_id), 2) AS avg_order_revenue FROM order_items AS item JOIN orders AS sales_order USING (order_id) JOIN customers AS customer USING (customer_id) WHERE sales_order.status != 'cancelled' GROUP BY customer.segment ORDER BY avg_order_revenue DESC, customer.segment LIMIT 5",
      "enabled": true
    },
    {
      "source_case_id": "success-015",
      "question": "非取消订单中，订单销售额不少于1000元的订单有哪些？列出订单、客户、状态和销售额，按销售额降序。",
      "sql": "SELECT sales_order.order_id, customer.customer_name, sales_order.status, ROUND(SUM(item.quantity * item.unit_price), 2) AS revenue FROM order_items AS item JOIN orders AS sales_order USING (order_id) JOIN customers AS customer USING (customer_id) WHERE sales_order.status != 'cancelled' GROUP BY sales_order.order_id, customer.customer_name, sales_order.status HAVING SUM(item.quantity * item.unit_price) >= 1000 ORDER BY revenue DESC, sales_order.order_id LIMIT 5",
      "enabled": true
    },
    {
      "source_case_id": "success-016",
      "question": "非取消订单中，每个销售渠道销售额最高的一笔订单是哪笔？列出渠道、订单号和订单销售额；并列按订单号升序，每个渠道只取一笔，最后按渠道升序。",
      "sql": "WITH order_revenue AS (SELECT sales_order.sales_channel, sales_order.order_id, ROUND(SUM(item.quantity * item.unit_price), 2) AS revenue FROM order_items AS item JOIN orders AS sales_order USING (order_id) WHERE sales_order.status != 'cancelled' GROUP BY sales_order.sales_channel, sales_order.order_id), ranked AS (SELECT sales_channel, order_id, revenue, ROW_NUMBER() OVER (PARTITION BY sales_channel ORDER BY revenue DESC, order_id) AS revenue_rank FROM order_revenue) SELECT sales_channel, order_id, revenue FROM ranked WHERE revenue_rank = 1 ORDER BY sales_channel LIMIT 5",
      "enabled": true
    }
  ]
}
