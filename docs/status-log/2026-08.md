# 项目状态历史日志：2026-08

> Append-only：只在文件末尾增加新回执；不得修改、删除或回写既有条目。

## 2026-08-02｜单文件状态账迁移快照

来源：`origin/main@e391aad7226691724cd52e265748e0d4e335d292:docs/status.md`；原文件 326 行，
Windows 工作树 SHA-256 为 `7f33f3fcb8b9e80fee8a25e0910afd44a97e4dd63980a82f0179bcb738d2f11a`。

<!-- snapshot-begin:e391aad7:docs/status.md -->
# 当前开发状态

> 本文件只保存当前事实、验证证据和下一检查点。稳定边界见 `PROJECT.md`。

| 字段 | 内容 |
| --- | --- |
| `state` | `in-review` |
| 更新时间 | `2026-08-02` |
| 当前切片 | `DOCKER-COMPOSE-READONLY-API-016`：Docker/Compose 本地闭环完成，远端发布验证进行中 |
| 最近完成 | `READONLY-API-HEALTH-015`：PR #11 已 squash 合并，主分支 CI 通过 |
| 做什么 | 固定依赖构建非 root 只读镜像，用 Compose 启动 health 与固定合成 run 回查 |
| 不做什么 | 不部署服务器，不调 Provider，不做鉴权、网页、Caddy/TLS、GHCR 或真实数据 |
| 完成门 | 本地已满足：镜像/Compose、health/run、双库哈希不变、只读安全姿态、55 项回归通过 |
| 风险 | 当前只有本地容器证据；没有 TLS、鉴权、持久化写卷或服务器运行证明，不可称为已上线 |
| 项目基线 | `main@2ce3c1c6`：PR #11 已 squash 合并；main CI run `30738237711` 成功 |
| 阻碍 | 无；远端分支已按本单授权发布，待 Draft PR 的 Python/container 双 CI 与 squash 合并 |

## 复用审查

- 已复用 Traceable 的 `api/src + api/tests` 布局、稳定事实/当前状态分离、失败关闭测试思路和
  只读 GitHub Actions 权限姿态。
- Traceable 没有可直接复用的 NL2SQL 只读执行器；本切片使用 Python `sqlite3` 的 URI
  `mode=ro`、`PRAGMA query_only` 和 authorizer 组成薄适配层。
- 暂不搬运其完整发布证明、Provider 预算、部署和多工作区治理；这些不属于当前完成门。

## 验证证据

- `PYTHONPATH=api/src python -m unittest discover -s api/tests -p "test_*.py" -v`：
  `Ran 8 tests in 0.259s`，`OK`。
- `python -m compileall -q api/src`：退出码 `0`。
- CLI 冒烟：创建 `.local/cli-demo.sqlite3` 后读回
  `tables=customers,order_items,orders,products`、`orders=6`。
- 测试内固定表行数为 `4 / 5 / 6 / 11`、聚合结果为 `5946.0`；7 类越界语句被拒绝，
  随后订单数和 4 表 schema 均未变化。
- `.github/workflows/ci.yml` 已配置 Python 3.13 编译和相同单测；远端当前结论以
  [GitHub Actions](https://github.com/suuny-ab/auditable-nl2sql-agent/actions/workflows/ci.yml)
  为准，不得把本地绿色写成远端 CI 已通过。
- 首次公开推送 SHA `429b44007e7848317fcccd3199a168ff97fc8075`；GitHub Actions
  [run 30628166219](https://github.com/suuny-ab/auditable-nl2sql-agent/actions/runs/30628166219)
  在该 SHA 上完成，结论为 `success`。

## DOCKER-COMPOSE-READONLY-API-016 验证证据

- 已读取本单，SHA-256 为
  `15a94f692fc7686491bc0bf2b3dcc3bc75e2b460237a44448c5273a272661395`；范围固定为本地
  Docker/Compose，不部署、不启用 Provider、不改产品代码。
- Docker `29.5.3` / Compose `5.1.4` 在 Linux/x86_64 daemon 上从固定 Python 3.13 digest 与
  hash lock 构建镜像 `sha256:8f42a490…f0613b`（`188522799` bytes）。
- `docker compose up --build --detach --wait` 成功；health 精确返回
  `health-v1 / 0.1.0.dev0 / ok / read_only=true`，list 总数为 `1`，
  `container-demo-run` 返回 `completed / run-record-v5`，POST 返回 `405`。
- 运行身份为 `10001:10001`，两份临时 SQLite 为 `0444:10001:10001`；根文件系统写入返回
  `Read-only file system`，Compose 实测 `read_only=true`、`cap_drop=ALL`、
  `no-new-privileges`、临时 `/tmp`，端口只绑定 `127.0.0.1:8000`。
- 初次直接从只读镜像路径打开 WAL checkpoint 复现 `sqlite3.OperationalError: unable to open
  database file`；隔离验证后由容器入口复制到 tmpfs 并设 `0444`，产品 reader 继续使用
  `mode=ro + query_only`。最终 health/list/detail/拒绝请求前后双库 SHA-256 完全不变。
- Python `3.13.12` 新增容器合同 2 项、全量 55 项测试通过；`compileall`、`pip check`、
  `docker compose config`、`git diff --check` 通过。`api/src` 差异、产品反向导入、容器凭据模式、
  `.local` 跟踪文件均为 `0`。
- CI 新增独立 `container` job，只构建和运行 Compose、验证相同合同并停止容器；不发布镜像、不
  部署、不读取凭据。本地绿色不等于远端 CI，远端证据须在 Draft PR 后补记。
- 实现提交为 `1316421ce457e7718c508c2b2423489869f67b6c`，基于
  `origin/main@2ce3c1c67e70e10b55d69d29f7346f146c6087bd`。首次 HTTPS 推送曾因 token 缺少
  `workflow` scope 被 GitHub 拒绝；用户补齐 scope 后，`gh auth status` 与 `gh api user` 确认
  `suuny-ab` 登录有效，远端 `agent/docker-compose-readonly-api` 已与本地恢复回执 head
  `8d13775e2b5f94e32440c50f4bf7b4ea688a06e2` 对齐。后续事实以 Draft PR 精确 head 为准。
- 本轮 Provider 调用、凭据读取、token 消耗、费用、服务器写入和公开部署均为 `0`。完整合同与
  Traceable 复用审查见
  [`docs/work/docker-compose-readonly-api.md`](work/docker-compose-readonly-api.md)。
- [PR #1](https://github.com/suuny-ab/auditable-nl2sql-agent/pull/1) 已合并为 `63381f9`；
  [main CI run 30684609206](https://github.com/suuny-ab/auditable-nl2sql-agent/actions/runs/30684609206)
  在该 SHA 上完成，结论为 `success`。

## READONLY-API-HEALTH-015 验证证据

- `auditable_nl2sql.server` 已把现有 `WorkflowRunReader` 绑定到 FastAPI lifespan；正常退出后再次
  查询固定失败为 `workflow runner is closed`，没有遗留可用写/读连接。
- README 中的模块命令在真实子进程以 Uvicorn 监听 `127.0.0.1`，health 返回
  `health-v1 / 0.1.0.dev0 / ok / read_only=true`，同一进程可回查完整 `run-record-v5`。
- 缺失业务库或 checkpoint 时在监听前失败且不创建文件；health 和 run 查询前后两份 SQLite
  SHA-256 均不变，应用没有注册 `POST/PUT/PATCH/DELETE` 路由。
- Uvicorn `0.52.1` 与 Windows 条件依赖 Colorama `0.4.6` 已精确固定到 pyproject、直接依赖输入
  和 hash lock；同一 Windows Python 3.13 环境 `pip check` 通过。
- Python `3.13.12` 启动器定向 3 项、全量 53 项测试通过；`compileall`、CLI help、
  `git diff --check`、`.local` 跟踪、可变路由和产品反向导入检查通过。
- 本轮 Provider 调用、凭据读取、token 消耗、费用、push 与 PR 均为 `0`。完整合同见
  [`docs/work/readonly-api-health.md`](work/readonly-api-health.md)。

## FASTAPI-READONLY-QUERY-014 验证证据

- `GET /api/v1/runs` 返回 `run-list-v1` 的稳定任务摘要和有界分页；
  `GET /api/v1/runs/{run_id}` 直接返回包含完整 trajectory 的 `run-record-v5`。
- 合法缺失 run 返回 `404`，非法 run ID 和分页返回 `422`，向两条路由发送 `POST` 返回
  `405`；应用未注册创建、审批、Provider 或数据库写入接口。
- 专用 `WorkflowRunReader` 以 SQLite `mode=ro + query_only` 打开既有 checkpoint，不暴露
  `run/decide`；底层写入尝试被 `readonly` 拒绝，缺失 checkpoint 不会被读取动作创建。
- 写入 runner 关闭后再构造 reader；构造前后、列表/详情/失败请求前后，合成业务库和 checkpoint
  SQLite 的 SHA-256 均不变，已有完成与挂起 run 的状态、trajectory 和执行次数不变。
- FastAPI `0.139.2` 已精确固定并写入 hash lock。Python `3.13.12` API 定向 4 项、全量 49 项
  测试通过；`compileall`、`pip check`、`git diff --check`、`.local` 跟踪和产品依赖方向检查通过。
- 本轮 Provider 调用、凭据读取和 token 消耗均为 `0`。完整合同见
  [`docs/work/fastapi-readonly-query.md`](work/fastapi-readonly-query.md)。
- [PR #10](https://github.com/suuny-ab/auditable-nl2sql-agent/pull/10) 的 head
  `974daf60a77f0e32b84488e769cdbe193a9a5200` 上 API job 成功，随后 squash 合并为
  `c1beba48e367facf3586fea84928192d0fb7afdd`；[main CI run 30737546034](https://github.com/suuny-ab/auditable-nl2sql-agent/actions/runs/30737546034)
  在该 merge commit 上完成，结论为 `success`。

## RESULT-EVIDENCE-006 验证证据

- 成功路径固定经过 `execute_sql → validate_result → bind_evidence → finish`；普通 run 与跨进程
  批准后的 run 均得到可由 `verify_evidence` 重算的 `evidence-v1`。
- 第二个真实 Python 进程按 run ID 回查 checkpoint 并重算 evidence 指纹，generator 与图节点
  均未重新执行。
- 创建 run 时的结果硬上限持久化在 state；以 5 行上限创建的挂起 run，在采用默认 100 行上限
  的新 runner 中恢复后仍只返回 5 行，并以 `failed/result_truncated` 停在 `validate_result`，不产
  evidence。
- 纯校验器对空列、行宽错误、截断、`NaN` 和 `bytes` 分别失败关闭；错误 SQL、未知问题、
  缺失 schema、审批拒绝和写 SQL 批准路径均不产 evidence。
- Python `3.13.12` 全量产品测试 25 项通过，`compileall`、`pip check` 与 `git diff --check`
  通过；成功、执行失败、写操作拒绝与截断路径均校验业务数据库哈希不变。
- 完整合同与声明边界见 [`docs/work/result-evidence.md`](work/result-evidence.md)。
  [PR #3](https://github.com/suuny-ab/auditable-nl2sql-agent/pull/3) 已合并为 `9f2b652`；
  [main CI run 30687356777](https://github.com/suuny-ab/auditable-nl2sql-agent/actions/runs/30687356777)
  在该 SHA 上完成，结论为 `success`。

## EVIDENCE-ANSWER-007 验证证据

- 成功路径固定增加 `compose_answer`：单值结果生成精确单元格引用，多行结果只陈述行数与字段，
  零行结果明确表示未返回数据。
- 回答前同时校验 evidence 指纹与完整绑定合同；指纹篡改、以及重新计算指纹但加入合同外字段的
  evidence 均失败关闭。
- 回答异常形成 `failed/evidence_verification_failed` 并停在 `compose_answer`；有效 evidence
  保留，answer 为 `null`。审批拒绝、写操作、错误 SQL、未知问题、缺失 schema 和截断路径也
  不产生 answer。
- 第二个真实 Python 进程按 run ID 回查的 answer 与原记录完全一致，且不重新执行图节点。
- Python `3.13.12` 全量产品测试 30 项通过；成功、回答失败和原安全失败路径继续验证业务库
  SHA-256 不变；`compileall`、`pip check`、差异与公开内容检查通过。完整合同见
  [`docs/work/evidence-answer.md`](work/evidence-answer.md)。
- [PR #4](https://github.com/suuny-ab/auditable-nl2sql-agent/pull/4) 已合并为 `c85fdb5`；
  [main CI run 30688678078](https://github.com/suuny-ab/auditable-nl2sql-agent/actions/runs/30688678078)
  在该 SHA 上完成，结论为 `success`。

## DEEPSEEK-PROVIDER-PROBE-009 验证证据

- `deepseek-v4-flash` 两轮各调用同一 3 条冻结案例，6 次响应均为 HTTP 200、
  `finish_reason=stop` 和严格 JSON；未自动重试。
- 首轮两条成功 SQL 的数值结果命中 gold，但列别名不稳定，因此首轮为 `FAIL`；加入显式
  `required_output_columns` 后复验为 `PASS`，两条成功结果的列、行和截断状态全部命中 gold。
- 删除请求由模型返回 `action=block`、`sql=null`，执行尝试为 0；本地畸形响应被严格解析器拒绝。
- 两轮合计 prompt `5051`、completion `529`、total `5580` tokens；代码层未增加费用限制。
- 两轮每个案例前后的业务库 SHA-256 均为 `564572c…1ea7`；key、Authorization header 和原始
  HTTP 包均未落盘，脱敏回执保存在 Git 忽略目录。
- Python `3.13.12` 全量产品与合同测试 33 项通过；`compileall`、`pip check`、
  `git diff --check` 和 tracked secret pattern scan 通过。完整合同见
  [`docs/work/deepseek-provider-probe.md`](work/deepseek-provider-probe.md)。
- [PR #6](https://github.com/suuny-ab/auditable-nl2sql-agent/pull/6) 已合并为 `604ccf4`；
  [main CI run 30691601003](https://github.com/suuny-ab/auditable-nl2sql-agent/actions/runs/30691601003)
  在该 SHA 上完成，结论为 `success`。

## DEEPSEEK-SQL-GENERATOR-010 验证证据

- `DeepSeekSqlGenerator` 默认禁用；只有 `from_environment(enabled=true)` 才读取
  `DEEPSEEK_API_KEY`。正式 HTTP transport 固定官方 HTTPS endpoint，无新增依赖或自动重试。
- 严格校验单一 choice、`finish_reason=stop`、usage 和精确 `action/sql/reason`；成功与模型决定
  均把 `provider-receipt-v1` 写入 `draft_sql` trajectory，不保存 key、header、原始包或隐藏思维。
- fake transport 覆盖 query、block/clarify/no_answer、畸形 envelope/JSON/usage、网络异常；这些
  失败路径执行次数为 0，不产生 approval、evidence 或 answer。
- Provider 即使把 `DELETE` 错标为 query，现有审批门仍给出 `can_execute=false`；模拟批准后以
  `approval_cannot_override_read_only` 结束，执行次数为 0，业务库哈希不变。
- 两条真实固定冒烟无重试：收入查询完成并得到 `5946.0`、可验证 evidence 与 `answer-v1`；删除
  请求以 `provider_blocked` 停在 `draft_sql`，执行次数为 0。prompt `1674`、completion `141`、
  total `1815` tokens。
- 真实冒烟前后业务 SQLite SHA-256 均为
  `564572c5667de341521fcf0405b1749bd240b7a7318e02bf11b8938cce491ea7`；脱敏回执只在 Git 忽略目录。
- Python `3.13.12` 全量产品与合同测试 41 项通过；`compileall`、`pip check`、
  `git diff --check`、tracked secret pattern scan、`.local` 未跟踪检查和产品不反向导入 `evals`
  检查通过。
- [PR #7](https://github.com/suuny-ab/auditable-nl2sql-agent/pull/7) 已合并为 `ee4758b`；
  [main CI run 30693856324](https://github.com/suuny-ab/auditable-nl2sql-agent/actions/runs/30693856324)
  在该 merge commit 上完成，结论为 `success`。

## PROVIDER-DECISION-PROBE-011 验证证据

- 4 条固定案例各调用一次且无自动重试，action 精确命中：`clarify`、`no_answer`、
  `unsafe_operation`、`block`；无 SQL 的三类均未进入工作流，执行次数为 0。
- 越权案例生成可审计的 `DELETE` SQL，现有机械审批得到 `can_execute=false`；模拟批准后仍以
  `approval_cannot_override_read_only` 结束，执行次数为 0，未产生 evidence 或 answer。
- 4 次调用的 prompt `3600`、completion `204`、total `3804` tokens；业务库 SHA-256 前后及
  逐案例均为 `564572c5667de341521fcf0405b1749bd240b7a7318e02bf11b8938cce491ea7`。
- 脱敏结构化回执只存在于 Git 忽略目录；没有保存 key、header 或原始响应。本探针未改产品代码、
  正式 prompt、依赖、评测运行器或指标口径。完整边界见
  [`docs/work/provider-decision-probe.md`](work/provider-decision-probe.md)。

## PROVIDER-DECISION-CONTRACT-012 验证证据

- 严格 plan 已支持 `query`、`unsafe_operation`、`clarify`、`no_answer`、`block`；SQL 是否必填由
  action 精确约束，未知 action 和字段漂移失败关闭。
- `run-record-v5` 投影 `provider_action`；三类无 SQL 决策得到 `clarification_required`、
  `no_answer`、`blocked` 稳定终态，执行次数为 0，且不产生 approval/evidence/answer。
- `unsafe_operation` action 自身强制进入 `reason=unsafe_operation`、`can_execute=false` 的审批；
  模拟批准后仍执行 0 次。反向错标为 `query` 的删除 SQL 仍由机械只读校验独立阻断。
- Python `3.13.12` Provider 定向 10 项、全量 43 项测试通过；编译、依赖、差异、凭据模式、
  `.local` 跟踪和产品依赖方向检查通过。本轮真实 Provider 调用与 token 消耗均为 0。完整合同见
  [`docs/work/provider-decision-contract.md`](work/provider-decision-contract.md)。
- [PR #8](https://github.com/suuny-ab/auditable-nl2sql-agent/pull/8) 已合并为 `0e960289`；
  [main CI run 30695247832](https://github.com/suuny-ab/auditable-nl2sql-agent/actions/runs/30695247832)
  在该 merge commit 上完成，结论为 `success`。

## MODEL-EVAL-RUNNER-013 验证证据

- 版本化 `model-evaluation-report-v1` 已实现：逐案例保存初始状态、模拟审批、最终 run record、完整
  trajectory、脱敏 usage、数据集/业务库哈希、判定理由和三个指标；报告与 checkpoint 均拒绝覆盖。
- 理想确定性 20 条恰好各运行一次，得到执行成功率 `8/8`、答案正确率 `20/20`、人工介入率
  `4/20`；3 条越权案例执行次数合计为 0，业务库逐案例及整轮哈希不变。
- 一条故意错误的注入查询使答案正确率精确降为 `19/20`，并记录一次非成功路径 SQL 执行，证明
  评测器不会把可执行但语义错误的模型输出计为正确。
- Python `3.13.12` 新增 2 项评测器测试、全量 45 项测试通过；编译、依赖、差异、凭据模式、
  `.local` 跟踪和产品依赖方向检查通过。
- 首次真实固定基线 `baseline-20260801T101307Z` 对 20 条冻结案例各调用一次，自动重试为 `0`；
  执行成功率 `7/8`、答案正确率 `14/20`、人工介入率 `7/20`。
- 20 条均有脱敏 usage：prompt `17664`、completion `1567`、total `19231` tokens。3 条越权案例
  执行次数均为 0，越权执行总数为 0；业务库整轮前后及逐案例哈希全部不变。
- 误差包括 1 条成功问题被判为无答案、3 条成功问题发生非预期审批，以及歧义/无答案各 1 条被
  错误路由为只读查询。结果按首次单次运行保留，没有调参、补跑或重复刷分。
- 完整指标口径、模拟审批边界与真实回执见
  [`docs/work/model-eval-runner.md`](work/model-eval-runner.md)。
- [Draft PR #9](https://github.com/suuny-ab/auditable-nl2sql-agent/pull/9) 已创建；
  [CI run 30695678436](https://github.com/suuny-ab/auditable-nl2sql-agent/actions/runs/30695678436)
  在 implementation head `91aaa20` 上完成，结论为 `success`。


## EVAL-DATASET-008 验证证据

- `evals/cases.jsonl` 恰好 20 条，固定 ID 与分类为成功 8、歧义 3、无答案 3、越权 3、注入 3；
  ID 和问题均唯一。
- 严格 JSONL 校验拒绝空行、未知字段、重复 ID、非法 `NaN`、类别数量漂移和 gold 字段漂移；
  成功与越权必须带参考 SQL，其余类别禁止预写 SQL。
- 8 条成功参考 SQL 经现有工作流复算：7 条直接完成，1 条 `LIMIT 11` 安全查询先挂起、批准后
  完成；列、行和截断状态全部与 gold 一致。
- 3 条越权参考 SQL 均先挂起且 `can_execute=false`，模拟批准后仍以
  `approval_cannot_override_read_only` 结束，执行尝试为 0，不产生 evidence 或 answer。
- 11 条参考案例复算前后业务数据库 SHA-256 不变；合同测试已进入现有 `api/tests` CI 入口。
- Python `3.13.12` 全量产品与合同测试 33 项通过；`compileall`、`pip check`、差异与公开内容
  检查通过。
- 当前数据集不是模型运行结果，尚无执行成功率、答案正确率或人工介入率。完整合同见
  [`docs/work/eval-dataset.md`](work/eval-dataset.md)。
- [PR #5](https://github.com/suuny-ab/auditable-nl2sql-agent/pull/5) 已合并为 `01db420`；
  [main CI run 30690523646](https://github.com/suuny-ab/auditable-nl2sql-agent/actions/runs/30690523646)
  在该 SHA 上完成，结论为 `success`。

## APPROVAL-GATE-004 验证证据

- SQLite `EXPLAIN QUERY PLAN` 与现有 authorizer 组成不执行结果计划的机械校验；写操作和
  `PRAGMA` 被拒绝，字符串策略只用于保守行数分类，不承担只读安全边界。
- `LIMIT 6` 在阈值 5 下挂起，第二个真实 Python 进程批准后只执行一次；拒绝、非法决定、
  缺失 run 和终态重复决定均失败关闭。
- 写 SQL 的审批状态固定 `can_execute=false`；即使批准也以
  `approval_cannot_override_read_only` 结束，执行尝试为 0，业务库哈希不变。
- 当前全量测试为 20 项本地通过；完整合同见
  [`docs/work/approval-gate.md`](work/approval-gate.md)。[PR #2](https://github.com/suuny-ab/auditable-nl2sql-agent/pull/2)
  已合并为 `d7be385`；
  [main CI run 30685924831](https://github.com/suuny-ab/auditable-nl2sql-agent/actions/runs/30685924831)
  在该 SHA 上完成，结论为 `success`。

## EVIDENCE-FINGERPRINT-PROBE-005 验证证据

- 两个独立 Python 进程对不同字典插入顺序的同一 evidence 得到相同的 2932 字节规范 JSON 和
  SHA-256 `b5522364…60dc`。
- SQL、schema、结果三类单字段变化全部产生不同指纹；截断、行宽错误、`NaN`、`bytes` 四类
  非法输入全部拒绝。
- 最终 7 项机器断言全部通过，业务库 SHA-256 始终为 `564572c5…1ea7`；未修改产品代码或依赖。
- 完整合同、指纹和声明边界见
  [`docs/work/evidence-fingerprint-probe.md`](work/evidence-fingerprint-probe.md)。

## 当前检查点

只读 API 查询与独立启动/health 已分别由 PR #10、PR #11 合入 `main`；Docker/Compose 切片已
完成本地实测，待按本单授权发布、取得 Python 与 container 两个远端 CI job 后 squash 合并。
保留首次真实 20 条基线的 `7/8`、`14/20`、`7/20`，不做 prompt 调优或补跑；服务器部署、
Caddy/TLS、鉴权与网页不自动开工。

## 审批中断/恢复最小风险探针

2026-08-01 在 Git 忽略目录完成，正式产品代码和依赖未变化：

- 进程 A 将高行数只读查询持久化为 `pending_approval` 并退出；进程 B 用同一 run ID 和
  `Command(resume=...)` 恢复为 `completed`，返回 11 行且独立执行账本恰好为 `1`。
- 第三个进程重复提交相同决定时没有重复执行，但 LangGraph 是静默 no-op，不会主动拒绝；
  正式 runner 因此需要显式终态检查和决定幂等合同。
- 独立拒绝 run 得到 `rejected`，执行计数为 `0`；批准和拒绝终态均无待处理任务。
- 最终 8 项机器断言全部通过，业务库 SHA-256 始终为
  `564572c5667de341521fcf0405b1749bd240b7a7318e02bf11b8938cce491ea7`。
- 完整合同与失败修正记录见
  [`docs/work/approval-interrupt-probe.md`](work/approval-interrupt-probe.md)。

## WORKFLOW-CORE-002 验证证据

- `langgraph==1.2.9`、`langgraph-checkpoint==4.1.1`、
  `langgraph-checkpoint-sqlite==3.1.0` 及全部传递依赖已从 hash 锁文件安装到全新
  Python `3.13.12` 虚拟环境；`pip check` 为 `No broken requirements found`。
- `PYTHONPATH=api/src python -m unittest discover -s api/tests -p "test_*.py" -v`：
  `Ran 16 tests in 1.862s`，`OK`；其中 7 条为工作流定向测试、8 条为原数据库回归、
  1 条钉定 pyproject / 顶层 requirements / hash 锁一致性。
- 成功 run 得到 `completed`、`5946.0` 和 `load_schema/draft_sql/execute_sql/finish` 四条
  有序 trajectory；失败路径固定自动重试 `0`，执行尝试最多 `1`。
- 独立第二 Python 进程使用会抛错的 generator 构造 runner，仍能只读回原 run，证明
  `get_run` 不会重新执行节点；这只证明持久化回查，不证明中断恢复。
- 成功、执行错误和写操作拒绝测试均比较业务库 SHA-256；workflow 表只存在于独立状态库。
- `compileall`、空白检查、公开内容扫描通过。PR #1 已合并，`main@63381f9` 的 Python 3.13
  远端 CI 已通过。

## LangGraph 最小风险探针

2026-07-31 在 Git 忽略的临时目录中完成，不包含正式产品代码或项目依赖变更：

- 隔离 Python `3.13.12` 成功安装 `langgraph==1.2.9`、
  `langgraph-checkpoint==4.1.1`、`langgraph-checkpoint-sqlite==3.1.0`。
- 第一进程运行 `draft_sql → execute_sql → finish` 三节点图，真实调用现有只读执行器，
  `probe-run-001` 得到 `completed`、`5946.0`、5 个 checkpoint 和 3 条 JSON trajectory。
- 第二个独立 Python 进程没有执行节点，通过公开 `get_state/get_state_history` API 从独立
  workflow SQLite 读回相同 run、终态、结果和 trajectory。
- 业务库探针前、第一进程后、第二进程后的 SHA-256 均为
  `564572c5667de341521fcf0405b1749bd240b7a7318e02bf11b8938cce491ea7`；仍为 4 表、6 笔订单。
- 结论：候选依赖、Python 3.13、严格 MessagePack、独立 SQLite checkpoint 和公开 API
  trajectory 投影的核心可行性已通过；失败终态、正式依赖锁和产品结构仍须在下一切片实现。
<!-- snapshot-end:e391aad7:docs/status.md -->

## 2026-08-02｜STATUS-TWO-LAYERS-017 开工回执

- 迁移基线为 `main@e391aad7`；实际旧文件为 326 行，不沿用派发说明中的早期 297 行快照。
- 当前层改为不超过 30 行；历史快照完整保存在本文件，后续回执只允许从文件末尾追加。
- 本切片不改产品、评测或 CI，不部署且不调用 Provider。

## 2026-08-02｜STATUS-TWO-LAYERS-017 初始远端回执

- Draft PR #13 建立于 head `2ca009b1786f0380a95fa9cdb924f36fa96d9fdc`，base 为
  `e391aad7226691724cd52e265748e0d4e335d292`。
- GitHub Actions run `30740473517` 的 `api` 与 `container` 两个 job 在该 head 上成功。
- 当前层进入 `ready-to-merge`；最终合并仍以追加回执提交的新 head 双 CI 为门。

## 2026-08-02｜STATUS-TWO-LAYERS-017 最终事实恢复

- PR #13 的最终 head `85b95fa6739a6c9153343c96b764771b86dc3e32` 在 run `30740528409` 的
  Python/container 双 job 全绿后 squash 合并为 `931bfa1479b03c41af62858750005ae4308a867a`。
- main CI run `30740588087` 在该 merge SHA 上再次双 job成功；本条只恢复已发生的远端事实，
  不把前一条当时的 `ready-to-merge` 瞬时状态留在当前层。

## 2026-08-02｜BUSINESS-KNOWLEDGE-LAYER-019 开工与本地证据

- 范围固定为两份合成知识文件、命中术语与相关字段备注的 Provider 上下文注入及冻结评测前后
  对比；不做枚举索引、训练对、偏好、记忆、技能、网页、工作流或审批门改造。
- 实现使用标准库严格加载 `10` 条唯一术语和 4 张表全部 `17` 个字段备注；fake transport 证明
  同义词命中、字段备注注入、稳定去重排序与无关知识排除，未命中时上下文为空。
- Python `3.13.12` 全量 `59` 项测试通过；`compileall`、依赖检查、两份 strict JSON、wheel 包含
  两份知识文件与 `git diff --check` 均通过，没有新增依赖。
- 一次性 20 条 DeepSeek 复跑已按授权档写入 `派发/授权请求.md`；获批前 Provider 调用、token、
  费用与评测报告均为 `0`。本地候选不先推送，避免评测回执造成未重新授权的第二次 push。
- 本地 Compose 前两次因 Windows CRLF shebang 以 `127` 退出；定位后把工作树脚本恢复为与 Git
  blob 相同的 LF，代码 diff 为零。第三次最终允许尝试达到 Healthy，health 为
  `ok/read_only=true`，固定 run 为 `container-demo-run/completed/run-record-v5`，并已清理独立项目。

## 2026-08-02｜BUSINESS-KNOWLEDGE-LAYER-019 唯一真实复跑

- 用户明文批准仅此一轮 20 次调用；`knowledge-20260802T100534Z` 对冻结 20 条各调用
  `deepseek-v4-flash` 一次，20 条均有 usage，自动重试为 `0`，未补跑、未调参、未刷分。
- 首次基线 → 知识层：执行成功率 `7/8 → 8/8`、答案正确率 `14/20 → 15/20`、人工介入率
  `7/20 → 7/20`；prompt `17664 → 20897`、completion `1567 → 1393`、total
  `19231 → 22290` tokens。完成“不低于 `14/20`”门，但同一冻结集不能证明泛化提升。
- 前后变化为：`success-002` 与 `ambiguity-002` 修正，`ambiguity-001` 退化；`success-007` 的结果列
  错误消失但仍有非预期审批，`success-006` 新增结果列/行错误。最终错误共 5 条，数字未修饰。
- 3 条越权案例执行次数与越权执行总数均为 `0`；两条非成功语义误判各执行一次只读 SQL。业务库
  整轮前后及逐案例 SHA-256 均为 `564572c5…1ea7`。
- Git 忽略报告为 `.local/evaluations/knowledge-20260802T100534Z/report.json`，SHA-256
  `666ce549956f69639b28fdac55d5b7781de42f0ffad8594e2cb22d9bc53e3102`；未保存凭据、header、原始包
  或隐藏思维。评测门通过，下一步只用原单授权推送当前候选并创建 Draft PR。

## 2026-08-02｜GOVERNANCE-RULES-SLIM-020 开工回执

- 已先同步 `origin/main@ff602261ff2d4b9d44a1adbf1b56e1c9b1f63e6c` 并创建隔离治理分支；原
  工作树的用户 `AGENTS.md` 改动保持原样，不混入本片。
- 范围固定为 `AGENTS.md` 索引化、3 份工程细则、切片目录索引与机器治理断言；产品、知识层、
  评测资产、依赖和 CI 合同不在范围内。
- Traceable PR #57 的 head `bddb35b7` 已合并且四个 required checks 全绿；本片只复用规则入口、
  单一授权 owner 和机器防漂移模式，不复制其产品 / HOLDOUT / 部署规则。
- `AGENTS.md` 从同步后已较短的 44 行变为 43 行索引；3 份工程细则承接原规则，未用机械删行制造
  降幅。治理脚本证明全部指针存在且授权 owner / 三档标记只在 `review.md`。
- Python `3.13.12` 全量 `60` 项测试、`compileall`、依赖、治理脚本与 diff 检查通过；`api/src`、
  `evals`、CI、知识 JSON、依赖和容器合同均未改变，Provider / 费用 / 部署为 `0`。

## 2026-08-02｜BUSINESS-KNOWLEDGE-LAYER-019 最终远端事实恢复

- PR #14 精确 head `b105238eb97af6adf55fe5fb0466528f0bd87846` 的 run `30743222647` 上
  `api` 与 `container` 双 job 成功，随后 squash 合并为
  `ff602261ff2d4b9d44a1adbf1b56e1c9b1f63e6c`。
- main CI run `30743266758` 在 merge SHA 上再次双 job 成功。本条只恢复已发生的远端事实，不改
  业务知识层产品、指标或报告。

## 2026-08-02｜DOC-GARDENER-021 开工回执

- 已从同步后的 `origin/main@4e9ec9ded60793dad12521aa7d7c9467ad44d042` 创建隔离分支；原工作树
  的用户 `AGENTS.md` 修改保持原样，不混入本片。
- Traceable PR #58 已合并为 `31541cfa`；本片只复用只读扫描、`stale` / `review` 分级、确定性
  报告和测试结构，按 NL2SQL 事实源重写规则，并把确定矛盾接入阻断门而非 advisory。
- 范围固定为园丁工具、治理检查、工程说明、首扫报告和测试；产品、评测、知识层、Provider、
  依赖、容器与定时任务不在范围内。

## 2026-08-02｜DOC-GARDENER-021 本地验证回执

- 活动文档扫描 `9` 个，结果 `stale=0/review=0`；手动全扫 `29` 个，结果
  `stale=0/review=7`。7 个历史相对时态已逐条写入首次报告，机器无法无损裁决，原文未改。
- 治理检查把活动文档 `stale` 作为失败门，CI 另有独立同命令 step；5 项园丁回归覆盖 7 类确定
  冲突、匹配主张、历史时间锚、报告确定性和 CLI 非零失败。
- 初次全量命令误用系统 Python 3.14，复现为环境缺少 checkpoint-sqlite；未改依赖，切换项目已有
  Python `3.13.12` 锁环境后全量 `65` 项通过。`compileall`、44 包依赖、Compose config、治理、
  diff 与禁改范围检查通过；Provider、费用、真实数据、产品、评测、知识层和部署变更均为 `0`。

## 2026-08-02｜WEB-SHOWCASE-REPLAY-022 开工回执

- 执行园丁单期间中心把派发入口原子替换为网页新单；归档前发现 SHA 从园丁开工值 `0EDAA77A…`
  变为 `3A8BA136…`，已把新单原样恢复到入口，并按捕获原文 / 原哈希归档园丁单，未吞掉新任务。
- 从同步后的 `origin/main@ecfbea247499302bfac9e7fe45d9d89e7524dbc8` 创建隔离网页分支；原
  工作树用户改动与园丁本地回执均未混入。本单范围固定为静态展示与真实回放，不部署。
- 按 `sites-building` capability path 在 `web/` 初始化独立站点并启动本地预览；Windows 下移除
  npm script 的 POSIX-only 环境变量前缀后可运行，不使用 hosting。
- 公开 `https://47.84.34.86/nl2sql/api/v1/health` 返回 `ok/read_only=true`；公开
  `container-demo-run` 返回 `run-record-v5/completed`、8 步 trajectory、结果 `5946.0`、
  `evidence-v1` 指纹与 `answer-v1`。两次均为只读 GET，Provider / token / 费用为 `0`。

## 2026-08-02｜WEB-SHOWCASE-REPLAY-022 本地验证回执

- 单页已展示一句话主张、边界、问题、SQL、结果 `5946.0`、证据指纹、回答、8 步 trajectory 与
  `assess_sql` 审批门；回放资产由现有产品 fixture 逐字段反向核对，不发请求创建 run。
- `npm test` 完成 Vinext 生产构建并通过 2 项 SSR / 脚手架合同；CI 新增独立 `web` job，用
  Node `24.13.0` 锁定安装后执行同一命令，不含发布步骤。
- Python `3.13.12` 全量 `69` 项通过；`compileall`、44 包依赖检查、治理、Compose config、
  `git diff --check` 通过。园丁 current `9/stale=0/review=0`，all `30/stale=0/review=7`。
- ImageGen 只生成与真实回放一致的 `web/public/og.png`；第一次因出现臆造日期被弃用，唯一一次
  修正后保留 SQL、结果和指纹均可回查的版本。没有 Provider、费用、部署或服务器写入。

## 2026-08-02｜TRAINING-PAIR-RETRIEVAL-024 开工回执

- 新派发单要求把冻结评测成功案例沉淀为训练对，并在 SQL 生成前轻量召回；明确排除向量库、
  工作流改造、Provider 调用与 20 条评测复跑。
- 已同步 `origin/main@03c84b6a` 并创建隔离分支 `agent/training-pair-retrieval`；原工作区用户
  `AGENTS.md` 修改保持原样，不混入本片。
- 范围冻结为版本化合成训练对、规范化字符二元组 Jaccard `0.72` 阈值、最多 2 条上下文注入，
  以及命中 / 未命中 / 禁用三条定向路径。产品代码不得导入 `evals`。

## 2026-08-02｜TRAINING-PAIR-RETRIEVAL-024 本地验证回执

- `training-pairs-v1` 收录 `success-001` 至 `success-008` 的问题、参考 SQL 与布尔开关；测试从冻结
  `evals/cases.jsonl` 独立读取后逐条相等，产品代码没有导入 `evals`。
- 召回使用规范化字符二元组 Jaccard，固定阈值 `0.72`、最多 2 条并稳定排序；相似问句以
  `0.8571` 命中，无关问题为空，禁用后的完全相同问题也为空。上下文升级为
  `business-context-v2`，模板明确不能覆盖 schema 与安全规则。
- 首轮全量中 73 项产品测试通过，仅治理因当前层尚未登记总数而失败；Web 因新工作树未安装
  `node_modules` 找不到 `vinext`。登记 `74` 项并执行锁定 `npm ci` 后，未改算法即全部通过。
- 最终 Python `74/74`、Web 构建 / SSR `2/2`、compileall、44 包依赖、strict JSON、Compose、
  治理和 diff 检查通过；wheel 包含三份合成知识 JSON，园丁 current `9/0/0`、all `31/0/8`。
  真实 Provider 调用、token、费用、评测复跑、数据库写入与工作流改动均为 `0`。

## 2026-08-02｜TRAINING-PAIR-RETRIEVAL-024 首次远端门回执

- 本单唯一 push 把 `agent/training-pair-retrieval@58584abedc94db7e277361a9cd7ec62fd99b279c`
  发布并创建 Draft PR #18；CI run `30749079569` 的 web 与 container 成功。
- API 在产品测试前由文档园丁失败：当前层写成“Python 全量 `74` 项、Web…”，不符合机器合同要求的
  “全量 N 项测试”连续措辞；不是产品、依赖、训练对、Provider 或容器失败。
- 修复只需把当前层改为“Python 全量 `74` 项本地测试通过”，但会形成新 head；本单一次 push
  授权已消费，因此先完成本地验证并请求精确新 head 的补充授权，不擅自推送或合并。
- 措辞修复后 Python 全量 `74/74`、Web 构建 / SSR `2/2`、治理、园丁 current `9/0/0` 与 diff
  检查再次通过；产品与算法未变。下一步只请求把这个本地精确 head 补推到现有 PR #18。

## 2026-08-02｜TRAINING-PAIR-FROZEN-EVAL-025 开工回执

- 用户批准仅一轮 20 条冻结合成案例真实 Provider 评测；自动重试 `0`，不补跑、不调参、不刷分，
  分数升降均须如实保存。
- 已从 `origin/main@d0b631553af4d24f01f1e89ddc24214dd9a733b9` 创建隔离分支
  `agent/training-pair-frozen-eval`；原工作区用户 `AGENTS.md` 修改保持原样。
- 首次报告复核为执行成功率 `7/8`、答案正确率 `14/20`、人工介入率 `7/20`，20 条 usage 共
  `19231` tokens，数据集哈希为 `c30a235…a3b6`；本轮 `>=14/20` 才进入远端流程，低于则不推送。

## 2026-08-02｜TRAINING-PAIR-FROZEN-EVAL-025 单次结果回执

- 零调用预检使用 Python `3.13.12` 与 44 个 hash-lock 包；冻结集仍为 20 条且 SHA-256 为
  `c30a235…a3b6`，新业务库哈希为 `564572c…1ea7`，checkpoint 与报告路径在开跑前均不存在。
- 唯一一轮 `trainpair-20260802T134352Z` 恰好保存 20 个唯一 case ID 与 20 条脱敏 usage，自动重试
  `0`，未补跑。执行成功率 `7/8 → 8/8`、答案正确率 `14/20 → 17/20`、人工介入率
  `7/20 → 4/20`；本轮 prompt `22530`、completion `1849`、total `24379` tokens。
- 四条成功案例由错转对，一条歧义案例由对转错；最终错误为 `ambiguity-001/002` 与
  `no_answer-001`，均误路由为只读查询。三条越权案例执行次数仍为 `0`，业务库整轮前后和逐案例
  哈希完全不变。
- Git 忽略报告 SHA-256 为 `5f54a9e7…5c2e6`，未发现凭据 / Authorization / Bearer 标记。因
  `17/20 >= 14/20`，满足进入已批准的推送、Draft PR 与 CI 路径；本轮 Provider 授权已经消费关闭。

## 2026-08-02｜INTENT-ROUTING-FIX-026 开工与根因回执

- 夜班单授权只修 `ambiguity-001/002` 与 `no_answer-001`，不改工作流结构、审批门或证据合同；
  唯一复跑最多 20 次 Provider 调用、自动重试 `0`、不补跑。
- 已从 `origin/main@797ed67dd2f203954ac2822feb34d4f5f7430789` 创建隔离分支
  `agent/intent-routing-fix`；原工作区用户 `AGENTS.md` 修改保持原样。
- 上轮报告 `trainpair-20260802T134352Z` 证明：通用销售额被默认解释为全时段；“最畅销”在金额与
  数量间缺指标；schema 不含 2026-01-05 至 2026-03-16 的数据覆盖事实，因而 2027 查询未被判无答案。
- 最小修复固定为 Provider transport 前的确定性意图门；三条命中不发网络请求，相邻限定充分的
  成功问题继续调用原 Provider。该方案只减少外部调用和只读误执行，不放宽任何安全边界。

## 2026-08-02｜INTENT-ROUTING-FIX-026 本地实现回执

- `intent-policy-v1` 已实现三条有界规则；命中时使用脱敏本地意图回执进入既有
  `clarification_required/no_answer` 终态，不构造 transport 请求，不修改 LangGraph 节点或终态合同。
- 三条原误路由的 fake transport 调用与 SQL 执行均为 `0`，业务库哈希不变；相邻限定充分的成功
  问题继续走 Provider。日期覆盖由 fixture 的 `MIN/MAX(order_date)` 测试锁定为
  `2026-01-05 / 2026-03-16`。
- Python `3.13.12` intent `5/5`、Provider `14/14`、全量 `81/81` 通过；编译与 44 包依赖检查
  通过。测试进程移除 Provider key，真实调用、token 与费用均为 `0`。

## 2026-08-02｜INTENT-ROUTING-FIX-026 单次评测回执

- 唯一一轮 `intentfix-20260802T142421Z` 对 20 条冻结案例各运行一次，自动重试 `0`、未补跑；
  其中 3 条由确定性意图门本地处理，真实 Provider 调用为 `17` 次。
- 执行成功率 `8/8 → 8/8`、答案正确率 `17/20 → 20/20`、人工介入率 `4/20 → 4/20`；错误
  案例 `3 → 0`，非成功类别 SQL 执行 `3 → 0`。上轮 / 本轮 total tokens 为 `24379 → 20821`，
  本轮 prompt `19367`、completion `1454`。
- 三条原误路由均保存 `provider_called=false` 的本地规则回执且执行次数为 `0`；20 条均判定正确。
  越权执行仍为 `0`，业务库整轮前后与逐案例哈希完全不变。
- Git 忽略报告 SHA-256 为 `bb1f6f2f…35f3fe`，未发现凭据或请求头标记。结果达到
  `20/20 >= 17/20`，进入夜班预授权的推送、Draft PR 和 CI 路径；本轮 Provider 授权已消费关闭。

## 2026-08-02｜FROZEN-EVAL-30-027 开工回执

- 新派发单只扩充 10 条未见合成题并运行一轮完整 30 题评测；不改知识层、训练对、意图门、生成
  逻辑或工作流，不补跑、不刷分，自动重试固定为 `0`。
- 已从 `origin/main@94a6715412f41a092464a3fd48990d916131b827` 创建隔离分支
  `agent/frozen-eval-30`；原工作区仅有的用户 `AGENTS.md` 修改保持原样，不混入本片。
- 原比例 `8/3/3/3/3` 按 1.5 倍扩充并离散为 `12/5/5/4/4`；新增量固定 `4/2/2/1/1`，额外余数
  给歧义与无答案，兼顾成功查询复杂度和语义失败关闭。
- 本单已明确授权最多 30 次 Provider 调用、一次推送和 Draft PR；题集与本地门全部冻结前不调用。

## 2026-08-02｜FROZEN-EVAL-30-027 题集冻结与本地证据

- 30 条合同得到 30 个唯一 ID / 问题，类别 `12/5/5/4/4`，数据集 SHA-256 为
  `66857af303ae1ac0010a077bf6beae8b46db1d92e7bf6398dcf9968686cb0a6a`；原 20 条规范化内容哈希
  保持 `773d0eae…64e4`。
- 4 条新成功题的多表聚合 gold 全部通过只读工作流复算；新歧义、无答案、越权与注入只标预期
  路由，新越权 / 无答案 reference SQL 均为空。训练对仍仅覆盖原 8 条，未修改知识或生成逻辑。
- Python `3.13.12` 全量 `81/81`、Web `2/2`、编译、44 包依赖、园丁 current `9/0/0`、治理、
  Compose config、strict JSONL、凭据与反向导入扫描、diff 检查均通过。
- 参考复算前后业务库 SHA-256 都为 `564572c5…1ea7`；此阶段真实 Provider 调用、usage 和费用为
  `0`。下一步仅创建全新运行目录并消费本单唯一 30 题授权。

## 2026-08-02｜FROZEN-EVAL-30-027 单次 30 题基线

- `unseen30-20260802T144417Z` 对冻结 30 条各运行一次，3 条旧题由本地意图门处理，真实 Provider
  usage `27` 条；自动重试 `0`，未补跑、未调参、未改题。
- 扩充后首份基线为执行 `12/12`、正确 `26/30`、介入 `9/30`；prompt `30577`、completion
  `2392`、total `32969` tokens。旧 20 题的 `20/20` 历史结果保持独立，未倒写。
- 新 10 题正确 `6/10`：6 条非成功路由全对且零执行；4 条成功都得到正确数据但都因缺有界
  `LIMIT` 发生非预期审批，其中 `success-009/011` 另有列别名不符，按合同如实判错。
- 4 条越权执行与全部非成功 SQL 执行均为 `0`；逐案例和整轮业务库哈希始终为
  `564572c5…1ea7`。报告 SHA-256 `7fbe51e8…d2404`，敏感标记扫描无命中。
- 本轮 Provider 授权已消费关闭，不再调用或补跑；下一步只用本单预授权推送最终文档 head、建
  Draft PR，三路 CI 全绿后按常设档 squash 合并。

## 2026-08-02｜ENUM-VALUE-INDEX-028 开工回执

- 新单只增加一份合成低基数枚举索引、问题命中与 SQL 生成上下文注入，再唯一复跑冻结 30 题；
  不改工作流、题面、gold、训练对或安全边界，不补跑、不刷分。
- 已从 `origin/main@64f380c8d9b8ac7a1f254688484f019fb7bc564e` 创建隔离分支
  `agent/enum-value-index`；原工作区仅有的用户 `AGENTS.md` 修改保持原样，不混入本片。
- 对比基线固定为执行 `12/12`、正确 `26/30`、介入 `9/30`，27 条 Provider usage 共 `32969`
  tokens；本轮只有正确率 `>=26/30` 才按预授权推送和创建 Draft PR。
- 设计范围只把合成 fixture 中封闭的地区、客户分群、商品类别、订单状态和销售渠道映射为规范
  库内值；`order_items` 的 ID、数量、价格不是枚举，不因当前样本低基数而索引。

## 2026-08-02｜ENUM-VALUE-INDEX-028 本地实现回执

- `enum-values-v1` 单文件覆盖四张合成表，索引 5 个封闭字段的 17 个真实 fixture 值；
  `order_items` 明确无枚举。严格加载拒绝未知字段、重复值 / 别名，文件 SHA-256 为
  `1db7eef4d8440cf4960e5ccd39d5a32fee908311c3db8ddb776e2d7b120c4245`。
- `business-context-v3.enum_values` 只投影问题实际命中且 schema 可用的规范值，最多 8 项；Provider
  prompt 把它限定为等值过滤提示，别名和元数据不能覆盖 schema、action 或安全规则。
- Python `3.13.12` 全量 `85/85`、Web `2/2`、编译、44 包依赖、4 份 strict JSON、wheel 包含四份
  知识文件、园丁 current `9/0/0`、治理和 Compose config 全绿。
- 冻结 30 题、原 8 条训练对、工作流、安全边界和依赖均未修改；本阶段真实 Provider 调用、usage、
  token 与费用为 `0`。下一步先冻结本地候选，再消费唯一 30 题授权。

## 2026-08-02｜ENUM-VALUE-INDEX-028 唯一 30 题复跑

- 实现先冻结为 `ea279fe72e05cb01c9933a72e11e0740b29050bd`；首个零调用预检因 blob 输出方式错误
  得到空副本并被合同拒绝，未创建 checkpoint / report、未触发 Provider。最终运行副本与基线
  SHA-256 同为 `66857af3…b0a6a`，规范化换行后与当前 Git blob 完全一致。
- `enum30-20260802T151131Z` 对 30 条各运行一次，3 条由本地意图门处理，真实 usage `27` 条；自动
  重试 `0`，未补跑、未调参、未改题。
- 基线 → 本轮：执行 `12/12 → 12/12`、正确 `26/30 → 26/30`、介入 `9/30 → 9/30`；30 条的
  正确性、action、初态、结果列与结果行均无变化，未证明指标提升。
- prompt / completion / total tokens 从 `30577/2392/32969` 变为 `32211/2262/34473`；错误仍为
  `success-009..012` 的非预期审批，其中 `009/011` 另有列别名不符，本片不越界修复。
- 4 条越权与全部非成功 SQL 执行均为 `0`，业务库逐案例与整轮哈希保持 `564572c5…1ea7`；报告
  SHA-256 `cc74943b…b19bd2`，敏感标记扫描无命中。因 `26/30 >= 26/30`，进入预授权远端流程。

## 2026-08-02｜UNSEEN-SUCCESS-FIX-029 开工与根因回执

- 新派发单只修上一轮暴露的 `success-009..012`，不改题本或工作流结构；本轮后这四题明确是
  开发集，不再声称未见。最多运行一次 30 题、自动重试 `0`，不补跑。
- 已从 `origin/main@57e462f9829fc81dd8445378bb07b0433a541441` 创建隔离分支
  `agent/unseen-success-fix`；原工作区仅有的用户 `AGENTS.md` 修改保持原样。
- 报告 `enum30-20260802T151131Z` 证明四题都执行成功并返回正确数据，但都因缺有界 `LIMIT` 触发
  非预期审批；`success-009/011` 另有 `month/quantity_count` 别名不符。
- 最小修复固定为扩充现有版本化训练对知识层：原 8 条保持不变，只新增四条已见开发集参考；不改
  召回算法、阈值、上限、Provider action、审批、只读执行或 evidence 合同。

## 2026-08-02｜UNSEEN-SUCCESS-FIX-029 首轮验证回执

- 四条开发集问题与 12 条连续覆盖合同共 5 条定向测试通过；每题以相似度 `1.0` 首位召回自身，
  SQL 均带 `LIMIT 5`，`009/011` 分别保留 `order_month/units_sold`。
- 首轮全量共运行 89 项，产品与新增知识测试全部通过；唯一失败为治理门
  `canonical_full_test_count_missing`，原因是活动状态层尚未登记新增后的全量测试数。
- 已按真实集合登记“全量 89 项测试”；本阶段移除了 Provider key，真实 Provider 调用、usage、
  token 与费用均为 `0`。下一步只重验治理门与全量，不借机修改实现。

## 2026-08-02｜UNSEEN-SUCCESS-FIX-029 本地实现回执

- `training-pairs-v1` 原 8 条逐字保持，只新增 `success-009..012` 四条已见开发集 reference；严格
  合同连续覆盖 12 条，文件 SHA-256 为 `70fec9d7…c2649e`。
- 四题均以相似度 `1.0` 首位召回自身，四条 SQL 都含 `LIMIT 5`；`009/011` 分别固定
  `order_month/units_sold`。题本、召回算法、工作流和 Provider adapter 无差异。
- 登记真实测试数后，Python `3.13.12` 定向 `5/5`、全量 `89/89` 通过；Web 构建 / SSR `2/2`、
  编译、44 包依赖、strict JSON、园丁 current `9/0/0`、治理、Compose 与差异检查全绿。
- wheel 成功包含四份知识 JSON；冻结题集 blob 与 `HEAD` 同为 `8abbe547…c84eece`。本阶段移除
  Provider key，真实调用、usage、token 与费用均为 `0`。

## 2026-08-02｜UNSEEN-SUCCESS-FIX-029 唯一 30 题复跑

- 候选先冻结为 `3775c85715cf79462fb02e09cdd1445d58e219e0`；运行副本题集 SHA-256 为
  `66857af3…b0a6a`，30 行内容与当前 Git blob 规范化后完全一致，新 checkpoint / report 预先不存在。
- `devfix30-20260802T153713Z` 对 30 条各运行一次，3 条由本地意图门处理，真实 usage `27` 条；
  自动重试 `0`，未补跑、未调参、未改题。
- 执行 `12/12 → 12/12`、正确 `26/30 → 30/30`、介入 `9/30 → 5/30`；四条已见开发集错误均从
  非预期审批变为直接完成，列别名与结果合同全部匹配。
- prompt / completion / total 从 `32211/2262/34473` 变为 `32774/2276/35050`；越权与全部
  非成功 SQL 执行均为 `0`，业务库哈希保持 `564572c5…1ea7`。
- 报告 SHA-256 `f1b9bd06…a6294d`，敏感标记扫描无命中。结果只证明对已见开发集的修复，不是新的
  泛化证据；因 `30/30 >= 26/30`，进入预授权推送与 Draft PR 流程。

## 2026-08-02｜FROZEN-EVAL-40-030 开工与设计回执

- 新派发单只把冻结合成评测从 30 扩到 40 并运行一次；不改知识层、训练对、意图门、生成逻辑、
  工作流或安全边界，自动重试 `0`，不补跑、不刷分。
- 已从 `origin/main@ee763129b259233b51e9c4944b597d963617a743` 创建隔离分支
  `agent/frozen-eval-40`；原工作区仅有的用户 `AGENTS.md` 修改保持原样。
- 新增量沿用离散比例 `4/2/2/1/1`，最终类别 `16/7/7/5/5`；新 ID 连续、零复用。前 30 条用
  规范化 SHA-256 `c229beea…be231a` 锁定。
- 难度聚焦多表关联、标价 / 成交价易混、`>=1000` 边界、组内 Top 1、近似歧义、缺失字段、
  分析包装写操作与间接提示注入；先完成合同与离线验证，期间 Provider 调用为 `0`。

## 2026-08-02｜FROZEN-EVAL-40-030 题集冻结与本地证据

- 严格合同现在包含 40 个唯一 ID / 问题，类别 `16/7/7/5/5`；新增 10 条为 `4/2/2/1/1`，全部
  非成功新增题 reference SQL 为空，逐题难度边界已登记。
- 按旧 30 个 ID 规范化后 SHA-256 保持 `c229beea…be231a`；完整 40 题规范化 SHA-256 为
  `c538bf96…97cef5`，当前文件 SHA-256 为 `b3f698bc…1072ef`。
- 四条新增成功 reference 由现有只读工作流复算，列 / 行 / 排序与审批合同全匹配；业务库前后
  SHA-256 均为 `564572c5…1ea7`。
- Python `3.13.12` 定向 `5/5`、全量 `89/89`、Web 构建 / SSR `2/2`、编译、44 包依赖、园丁
  current `9/0/0`、治理、Compose 与差异检查全绿。
- 知识层、12 条训练对、Provider、意图门、工作流与依赖零差异；本阶段真实 Provider 调用、usage、
  token 与费用为 `0`。下一步只冻结候选并做唯一 40 题运行预检。

## 2026-08-02｜FROZEN-EVAL-40-030 唯一 40 题新基线

- 候选先冻结为 `6e6874d89b83763b248ed9a179661daf7ad9c5fa`；运行副本题集 SHA-256 为
  `b3f698bc…1072ef`，40 个 ID / 问题唯一，新业务库、checkpoint 与 report 路径预先不存在。
- `unseen40-20260802T155929Z` 对 40 条各运行一次，3 条由本地意图门处理；37 条进入 Provider
  transport，35 条保存 usage，`success-010/011` transport 失败且无 usage。自动重试 `0`，未补跑。
- 新基线为执行 `14/16`、正确 `32/40`、介入 `10/40`；旧 30 题本轮 `28/30`，新增 10 题
  `4/10`。成功 / 歧义 / 无答案 / 越权 / 注入正确数为 `10/16、5/7、7/7、5/5、5/5`。
- 新四条成功均因无 `LIMIT` 非预期审批，`013/014` 另有别名 / 数值差异；两条新近似歧义被判
  `no_answer`。prompt / completion / total 为 `42410/3210/45620` tokens。
- 越权与全部非成功 SQL 执行均为 `0`，业务库哈希保持 `564572c5…1ea7`；报告 SHA-256
  `cba3eadc…403eec8`，敏感标记扫描无命中。该轮作为 40 题独立基线封存，不补跑、不修题。

## 2026-08-03｜HARDCASE-FIX-031 开工与根因回执

- 新派发单只修 40 题基线里的 6 条新增错例；`success-013..016` 进入现有训练对知识层，
  `ambiguity-006..007` 进入 Provider transport 前的确定性意图门。不改题本或工作流结构。
- 已从 `origin/main@42e7edaa25ae75c2479fe8555a602e1c93d8bf74` 创建隔离分支
  `agent/hardcase-fix`；原工作区用户 `AGENTS.md` 修改保持原样。
- 四条成功题都因缺 `LIMIT` 触发非预期审批，`013` 另有别名差异，`014` 另有数值舍入差异；两条
  歧义题因缺少折扣指标或复购定义 / 指标被误判为无答案。
- `success-010/011` 的旧题 transport 失败且无 usage，按派发单只记环境抖动，不修 transport、
  不补跑。本轮最多 40 次 Provider 调用、自动重试 `0`，完整冻结复跑只允许一次。

## 2026-08-03｜HARDCASE-FIX-031 本地实现回执

- `training-pairs-v1` 原 12 条逐对象保持不变，只追加 `success-013..016` 四条已见开发集 reference；
  文件 SHA-256 `a2c5d2c6…29be79`，四题都以相似度 `1.0` 首位召回并带 `LIMIT 5`。
- `intent-policy-v2` 增加折扣指标与复购定义两条有界澄清规则；两条错例的 fake transport / SQL 执行
  均为 `0`，相邻限定充分的问题继续走 Provider，执行安全边界未改。
- Python `3.13.12` 新增定向 `6/6`、全量 `95/95`，Web `2/2`、编译、44 包依赖、strict JSON、
  wheel、园丁 current `9/0/0`、治理、Compose 与差异门全部通过。
- 冻结题集 Git blob 与 `origin/main` 完全相同，工作流、Provider adapter 和依赖无差异；测试进程
  移除 Provider key，本阶段真实调用、usage、token 与费用为 `0`。

## 2026-08-03｜HARDCASE-FIX-031 唯一 40 题复跑

- 候选先冻结为 `b8961614eeb99d047e3bcab355696d278e2e5513`；唯一轮次
  `hardfix40-20260802T162815Z` 的输入 SHA-256 为 `b3f698bc…1072ef`，40 个 ID / 问题唯一。
- 5 条由本地意图门处理，35 条进入 Provider transport 且全部保存 usage；自动重试 `0`，未补跑。
  执行 `14/16 → 16/16`、正确 `32/40 → 40/40`、介入 `10/40 → 6/40`。
- 6 条开发集错例全部判对；两条旧题 `success-010/011` 本轮 transport 正常，只记上轮环境抖动。
  五类正确数为 `16/16、7/7、7/7、5/5、5/5`。
- prompt / completion / total 从 `42410/3210/45620` 变为 `43701/3243/46944`；越权与全部
  非成功 SQL 执行均为 `0`，业务库前后保持 `564572c5…1ea7`。
- 报告 SHA-256 `72475050…c29758`，敏感模式无命中。本轮只证明已见开发集修复；Provider 授权已
  消费关闭。因 `40/40 >= 32/40`，进入预授权推送与 Draft PR 路径。

## 2026-08-03｜SCHEMA-HOLDOUT-032 开工与冻结设计

- 方向从扩题 / 修题循环切换为 schema 泛化：本片只建第二合成库、映射 15 个同题并运行一次
  HOLDOUT；不改知识、训练对、prompt、产品工作流或现有 40 题。
- 已从 `origin/main@6372713ac69a54d8b8a112ea79f3e80176e1e6d9` 创建隔离分支
  `agent/schema-holdout`；原工作区 head 与用户 `AGENTS.md` 修改保持原样。
- 新库冻结为三表：买方维、商品维、合并订单头的交易行事实；表 / 字段名与主库零重合，金额改整数分，
  状态与渠道改码。15 题类别固定 `7/2/2/2/2`。
- 主库同题对照为正确 `15/15`、成功执行 `7/7`、介入 `2/15`；新库首次数字无论高低都封存，最多
  15 次 Provider 调用、自动重试 `0`、不补跑、不按结果调优。

## 2026-08-03｜SCHEMA-HOLDOUT-032 离线实现回执

- 第二 fixture 的三表 / 字段与冻结设计逐项相等，并与主库表 / 字段名零重合；行数 `4/5/11`、
  唯一订单 `6`，确定性数据库 SHA-256 为 `ed9a2cda…78143d`，已有目标拒绝覆盖。
- 15 题映射原始 / 规范化 SHA-256 为 `3a598167…7bb16 / 123c8317…779cd`；类别
  `7/2/2/2/2`，问题 / expected 与主库同题相等，8 条 reference 只使用新表。
- 7 条成功 gold 经原只读工作流复算全对，2 条越权失败关闭，业务库哈希不变；默认评测合同仍拒绝
  15 题，只有显式 `schema-holdout-v1` 放行。
- Python `3.13.12` 定向 `5/5`、全量 `100/100`，Web `2/2`、编译、44 包依赖、strict JSONL、
  园丁、治理、Compose 与差异门全绿；产品知识、训练对、意图、Provider、工作流和主 40 题零差异。
- 一次重验中产品 100 项已执行，仅治理因活动状态“100/100”措辞无法提取总数；只改成机器要求的
  “全量 100 项测试通过”后重跑，全量与治理均绿，未修改实现或 HOLDOUT。
- 本阶段真实 Provider 调用、usage、token 与费用为 `0`；下一步只冻结候选并做唯一运行预检。

## 2026-08-03｜SCHEMA-HOLDOUT-032 首次且唯一泛化基线

- 候选先冻结为 `69914a13305665a2ee4428f1128f94e3c58ba8ed`；唯一轮次
  `schema15-20260802T165212Z` 的 HOLDOUT / 第二库 SHA-256 分别为
  `3a598167…7bb16 / ed9a2cda…78143d`，新 checkpoint / report 预先不存在。
- 15 题各进入 Provider transport 一次且保存 usage；transport 失败 `0`、自动重试 `0`、未补跑。
  主库同题 → 换 schema 为执行 `7/7 → 0/7`、正确 `15/15 → 6/15`、介入 `2/15 → 2/15`。
- 主库同题 / HOLDOUT 的 prompt、completion、total 为
  `19192/1541/20733 → 16434/999/17433`；五类正确数为 `0/7、0/2、2/2、2/2、2/2`。
- 7 条成功与 2 条歧义均被保守判为 `no_answer`；所有 SQL 执行为 `0`，越权执行为 `0`，业务库
  逐案例和整轮哈希不变。报告 SHA-256 `c3fe554c…ee15c8`，敏感模式无命中。
- `6/15` 是首次且最终 HOLDOUT 基线，直接暴露当前 schema 泛化不足；不调优、不补跑。Provider
  授权已消费关闭，下一步只提交脱敏结果并进入预授权远端流程。

## 2026-08-03｜SCHEMA-KNOWLEDGE-BUILDER-033 开工与边界

- 新单只从 schema 元数据规则式生成字段备注初稿和候选术语，接入现有生成链后唯一复测第二库
  15 题；不改考场、单题、训练对、意图门、工作流或安全边界，不补跑、不刷分。
- 已从 `origin/main@f6d30aa2d01d624db08759fc3a61564b8e7a59cc` 创建隔离分支
  `agent/knowledge-layer-builder`；原工作区 head 与用户 `AGENTS.md` 修改保持原样。
- 对比基线固定为执行 `0/7`、正确 `6/15`、介入 `2/15`，15 条 usage 共 `17433` tokens；本轮最多
  15 次 Provider 调用、自动重试 `0`，第一次完整结果无论升降即封存。
- 复测仍读取上一轮已验证的 LF 冻结输入 `3a598167…7bb16`；不通过当前 Windows checkout 的换行
  差异改变题目内容或考场字节。

## 2026-08-03｜SCHEMA-KNOWLEDGE-BUILDER-033 本地实现回执

- `schema-derived-knowledge-v1` 从表 / 字段标识符、声明类型、主键和外键生成稳定的字段备注与候选
  术语；第二库全部 16 个字段被覆盖，术语引用全部落在输入 schema 内，畸形字段与悬空外键失败关闭。
- 主库完整 / 子集 schema 继续使用原静态知识、枚举和训练对；出现未覆盖字段才启用自动知识，第二库
  的 Provider 请求已由 fake transport 证明不携带引用旧表的训练 SQL。
- 自动知识定向 `6/6`，连同主库知识与 Provider 合同共 `39/39`；Python `3.13.12` 全量 106 项测试
  通过，Web `2/2`、编译、44 包依赖、strict JSONL、园丁 current `9/0/0` 和治理全绿。
- 完整 Compose 本地验收确认 `10001:10001`、health `read_only=true`、固定 run 完成、POST 为 `405`，
  业务库 / checkpoint 哈希前后相等；首次 Windows CRLF 启动以 127 失败后只规范化本地入口脚本，
  第二次成功，最终脚本代码 diff 为零并恢复原检出换行。
- 40 题、第二库、HOLDOUT、16 条训练对、意图、Provider adapter、工作流、依赖与 Compose 相对
  `origin/main` 零差异；本阶段真实 Provider 调用、usage、token 与费用为 `0`。

## 2026-08-03｜SCHEMA-KNOWLEDGE-BUILDER-033 唯一换库复测

- 候选先冻结为 `11b247773927e8d321e8fba3250070f63d05a381`；唯一轮次
  `autoknowledge15-20260802T172429Z` 的 HOLDOUT / 第二库 SHA-256 分别为
  `3a598167…7bb16 / ed9a2cda…78143d`，新 checkpoint / report 预先不存在。
- 15 题各有一条脱敏 usage，transport 失败 `0`、自动重试 `0`；未补跑、未调参、未修改代码、第二库、
  问题、gold 或判分器。基线 → 本轮为执行 `0/7 → 3/7`、正确 `6/15 → 8/15`、介入
  `2/15 → 5/15`。
- prompt / completion / total tokens 从 `16434/999/17433` 变为 `19635/1276/20911`；五类正确数
  从 `0/7、0/2、2/2、2/2、2/2` 变为 `0/7、2/2、2/2、2/2、2/2`。
- 两条歧义题从错误 `no_answer` 恢复正确澄清；7 条成功仍 `0/7`。`success-004/007/013` 各执行
  一次只读 SQL并模拟审批，但因错误状态码、金额单位 / 结果列行和无界审批判错；另外四条成功题以
  `no_answer` 或 `clarify` 保守终止。
- 越权与全部非成功 SQL 执行均为 `0`，全部三次执行都来自成功类别；业务库逐案例与整轮哈希保持
  `ed9a2cda…78143d`。报告 SHA-256 `40ebf5b8…11f8c3`，敏感模式命中 `0`。
- `8/15` 只证明局部路由改善，成功查询泛化仍未成立；Provider 授权已消费关闭，不据结果修改构建
  规则、考场或补跑。下一步只提交脱敏结果并进入预授权远端流程。

## 2026-08-03｜PARAPHRASE-EVAL-034 开工与离线合同

- 本片只从主库 40 题选 10 题，每题冻结 3 种含义不变的自然改述并运行唯一基线；不改主题、知识层、
  训练对、意图、Provider 或工作流，不按掉分调优，不补跑、不刷分。
- 已从 `origin/main@e75c60e6f0b3a3bd22a88b8a2a4f561d721d85e9` 创建隔离分支
  `agent/paraphrase-baseline`；原工作区 head 与用户 `AGENTS.md` 修改保持原样。
- 来源按五类各 2 题固定，10 道原题在封存轮次 `unseen40-20260802T155929Z` 中正确 `8/10`；
  每组 `formal / colloquial / restructured` 三种改写，共 30 个唯一问题。
- `paraphrase-dataset-v1` 保存含义不变声明、来源映射和原题判定；物化合同严格复制来源类别、reference
  SQL 与 expected，并锁定主 40 题 LF SHA-256 `dca2a3a0…c6a794`。
- 改述定向 `4/4`、Python `3.13.12` 全量 110 项、Web `2/2`、编译、44 条锁依赖、strict JSON、
  园丁、治理、Compose config、凭据与反向导入检查全绿。
- 完整 Compose 本地验收确认非 root `10001:10001`、health 只读、固定 run 完成、POST 为 `405`，
  两库哈希前后相等；验证后容器移除，临时 LF 入口恢复原字节且代码 diff 为零。
- 知识、训练对、意图、Provider、工作流、产品依赖、主 40 题与两项旧泛化考场均未修改。截至当前
  真实 Provider 调用、usage、token 与费用为 `0`；冻结候选后才消费唯一 ≤30 次授权。

## 2026-08-03｜PARAPHRASE-EVAL-034 唯一改述基线

- 候选冻结为 `9d664dd0fff50d31a8d92e3705c3772f445e39b0`；唯一轮次
  `paraphrase30-20260802T174944Z` 的 business / checkpoint / report / 题集副本路径预先不存在，
  30 个 case ID 与问题唯一，自动重试 `0`，未补跑、未改题、未调优。
- 选中原题正确率 `8/10`，改述正确率 `24/30`；逐变体与原题结果一致 `24/30`，完整稳定来源
  `8/10`。聚合均为 `80%`，但有一组全掉分和一组全改善，不能描述为逐题完全稳定。
- `ambiguity-001-p1..p3` 从原题正确澄清掉为错误 query，产生三次非成功只读执行；
  `ambiguity-006-p1..p3` 从原题错误改善为正确澄清。`success-013-p1..p3` 延续原题错误，均有
  非预期审批与列 / 行不符；其余 7 个来源的三种改述都保持原题结果。
- 五类正确数为 `3/6、3/6、6/6、6/6、6/6`；成功执行 `6/6`、介入 `9/30`，越权执行 `0`。
  三次非成功执行都来自无范围销售额改述，业务库逐案例与整轮保持 `564572c5…1ea7`。
- 26 条真实 usage 的 prompt / completion / total 为 `30367/2099/32466`；另外 4 条由本地意图门
  终止，transport 失败 `0`。报告 SHA-256 `bf28559c…7cec10`，敏感模式命中 `0`。
- 唯一 Provider 授权已经消费关闭，不据结果修改知识、训练对、意图、Provider、工作流或题集；
  下一步只提交脱敏结果并进入预授权远端流程。

## 2026-08-03｜PARAPHRASE-SYNONYM-COVERAGE-035 开工与根因

- 本片只修 `ambiguity-001-p1..p3` 暴露的通用销售额同义表达 / 句式覆盖；不改 30 题考场、训练对、
  Provider、工作流或安全边界，不重跑其余 27 题，不补跑、不刷分。
- 已从 `origin/main@b70dbb4f5e44206c884f21678cfff188d5c3cc4a` 创建隔离分支
  `agent/paraphrase-synonym-coverage`；原工作区 head 与用户 `AGENTS.md` 修改保持原样。
- 基线 trajectory 证明三题均绕过 `revenue-scope-required`：p1 / p3 已命中旧业务术语但残留通用
  请求包装，p2 的“卖了多少钱”完全不命中旧销售额词项；不是 schema、SQL 或 transport 故障。
- 最小修复只给“销售额”补 4 个通用别名，并给既有判断补相同词项与 6 个填充词；没有新增判断分支、
  case ID、整句或结果逻辑。
- 三条冻结掉分题、两个邻近表达、主库全部 16 条成功题及 4 条带范围邻近问题定向通过；改述复测
  合同只选择原冻结三题。定向 `36/36`、Python `3.13.12` 全量 115 项测试通过。
- 零 success 子集的执行率按 `0/0, value=null` 记录为不可适用；集成测试证明三题均本地澄清且
  transport 调用为 `0`，修复的是汇总边界，不重跑任何真实轮次。
- Web `2/2`、编译、44 条锁依赖、strict JSON、园丁、治理、Compose config、凭据 / 反向导入与
  差异门全绿；完整 Compose 验收确认非 root、只读 health、固定 run 与两库哈希不变。
- 截至当前真实 Provider 调用、usage、token 与费用为 `0`；30 题考场、主 40 题、训练对、Provider、
  工作流和依赖无差异。冻结候选后才消费唯一最多 3 次授权。

## 2026-08-03｜PARAPHRASE-SYNONYM-COVERAGE-035 唯一复测

- 候选冻结为 `74a99644b417ae88ec4f381321c825f30c120c04`；唯一轮次
  `revenuepara3-20260802T181238Z` 只选择原冻结掉分三题，路径预先不存在，自动重试 `0`，没有
  补跑、换题或据结果调优。
- 三题均由 `intent-policy-v3` 在本地返回 `clarify + revenue-scope-required`，所选正确率
  `0/3 → 3/3`，投影完整改述 `24/30 → 27/30`（`+3`）；非成功 SQL 执行从基线三次降为本轮
  `0`，越权执行 `0`，业务库哈希不变。
- 三条 checkpoint 完成后，旧汇总器因 success 类分母为零退出；没有重跑工作流，只从既有 checkpoint
  只读恢复标准报告，并补 `0/0, value=null` 集成回归合同。真实 Provider 调用 / usage / token
  均为 `0`，报告 SHA-256 `18801b7b…6b491e`，敏感模式命中 `0`。
## 2026-08-03｜SCHEMA-SUMMARY-INJECTION-036 开工与根因

- 本片只增加通用 schema 紧凑结构投影并运行第二库 15 题唯一复测；不改单题、考场、训练对、知识、
  多轮澄清、安全边界或判分器，不补跑、不刷分。
- 已从 `origin/main@1e9756e7e76e6388c1e0c415efe24c4f955ee4f1` 建立隔离分支
  `agent/schema-summary-injection`；原工作区 head 与用户 `AGENTS.md` 修改保持原样。
- 现有 Provider user input 已包含完整 `schema_snapshot`，所以本片是紧凑注意力投影，不是首次提供
  schema。自动知识基线 `autoknowledge15-20260802T172429Z` 为执行 `3/7`、正确 `8/15`、介入
  `5/15`，成功类仍 `0/7`；剩余错误还涉及未知状态码、金额单位 / 输出与无界审批。
- 冻结资产与代码改动前真实 Provider 调用、usage、token 与费用均为 `0`；唯一复测上限 15 次、
  自动重试 `0`，结果升降均不补跑。
- 通用摘要红灯先证明实现不存在；修复后两库与相关主 40 离线合同 `44/44` 通过。摘要为主库
  `4 表/17 字段/701 字符`、第二库 `3 表/16 字段/631 字符`，不读取业务行或提供枚举值。
- 首次全量发现 120 项中的治理子测只因当前状态缺少新测试总数而按合同失败；其余 119 项通过，
  未修改代码或放宽治理，先补机器事实再重跑。
- 写入机器总数后，Python `3.13.12` 全量 `120/120`、Web `2/2`、编译、锁依赖、strict JSON、
  园丁、治理、Compose config、凭据 / 反向导入与差异门全绿。
- 完整 Compose 验收确认非 root `10001:10001`、只读 health、固定 run、POST `405` 与两库哈希不变；
  容器移除，入口脚本恢复原 SHA 且不形成代码差异。真实 Provider 调用仍为 `0`。

## 2026-08-03｜SCHEMA-SUMMARY-INJECTION-036 唯一换库复测

- 候选 `16d92c703fa6949c5c6e17baddcc337c246d23a8` 冻结后，唯一轮次
  `schemasummary15-20260802T184047Z` 使用未变的第二库和 15 题 HOLDOUT；新 run root / checkpoint /
  report 预先不存在，15 个 ID / 问题唯一，未补跑或调优。
- 基线 → 本轮为执行 `3/7 → 2/7`、正确 `8/15 → 8/15`、介入 `5/15 → 4/15`；成功类仍
  `0/7`，其余四类各 `2/2`。摘要未提高总正确或成功查询泛化，执行率下降一题。
- 15 条 usage 的 prompt / completion / total 为 `19635/1276/20911 → 23925/1367/25292`，
  transport 失败和自动重试均为 `0`。两条 query 仍因无界审批、金额单位 / 输出不符判错，其余成功
  题因未知状态 / 渠道值保守终止。
- 越权与全部非成功 SQL 执行均为 `0`，业务库哈希不变；报告 SHA-256 `960f6c20…abd35a`，
  敏感模式命中 `0`。唯一复测授权已消费关闭，不为抬分修改候选或考场。
## 2026-08-03｜SHOWCASE-VALIDATION-ARC-037 开工与证据冻结

- 本片只改静态展示页：五步调优弧线、泛化三维、短板与候选改道；不改产品 / 评测、不调用 Provider、
  不新增交互能力、不部署。
- 已从 `origin/main@99ec46d0e27f87619f11cf0898a59e474a4de121` 创建隔离分支
  `agent/showcase-validation-arc`；原工作区 head 与用户 `AGENTS.md` 修改保持原样。
- 五步核真为 `14/20 → 17/20 → 20/20 → 30/30 → 40/40`，对应 PR #9 / #19 / #20 /
  #23 / #25；前三步同一 20 题，后两步也是观察错误后修复的主库开发集，不能称未见泛化。
- 三维固定为主库已见开发集 `40/40`、换 schema `8/15` 且成功题仍 `0/7`、同义改述投影
  `27/30` 且只复跑三题；对应 PR #25 / #30 / #29。所有链接只指向公开版本化回执与 PR。

## 2026-08-03｜SHOWCASE-VALIDATION-ARC-037 本地候选完成

- SSR 红灯先因 `14/20` 不存在而失败；实现后五步弧线、三维泛化、`0/7` 短板与七组报告 / PR
  双链接进入页面，开发集、投影和候选假设边界由 SSR 合同锁定。
- Python `3.13.12` 全量 `120/120`、Web 构建 / SSR `3/3`、lint、编译、44 条锁依赖、strict JSON、
  园丁、治理、Compose config、凭据 / 反向导入与 diff 门全绿；产品、评测、依赖和部署 diff 为零。
- 新 OG `1536×1024`、SHA-256 `DBCD5FB3…BB8AE7` 且文字核验通过。公网仍为 v1，本轮 Provider、
  评测、费用、服务器写入与部署均为 `0`；v2 只进入远端代码评审。

## 2026-08-03｜METRICS-CARD-038 开工与口径冻结

- 从 `origin/main@d50f510214fbc1f57cf42da467df28f473a76cd1` 建隔离分支
  `agent/metrics-card`；原工作区 head 与用户 `AGENTS.md` 修改保持原样。
- 范围冻结为已有调优五步、泛化三维、换库 `0/7`、改述正确 / 稳定、单轮调用上限、有界候选 / 审批
  参数和测试规模；不新增或重算指标，不调用 Provider、不运行评测、不改代码或部署。
- 派发单 SHA-256 为 `9FE2757C…A391`。金额预算和修复后完整稳定率没有仓库数字，必须明确写“没有”，
  不能推算；每个登记数字同时绑定报告 / 工作流与 PR。

## 2026-08-03｜METRICS-CARD-038 本地候选完成

- 口径卡形成 34 个数字表格行；17 个核心口径、16 个相对证据文件和 16 个已合并 PR 经机器审计
  通过。README 增加唯一入口，每行均写明定义、方法、对象、复跑属性与“不是什么”。
- Python `3.13.12` 全量 `120/120`、Web 构建 / SSR `3/3`、lint、编译、44 条锁依赖、strict JSON、
  园丁、治理、Compose config、凭据 / 反向导入与 diff 门全绿。
- 相对 `origin/main` 的产品、测试、评测、依赖、CI、Web 与部署差异均为 `0`；Provider 调用、评测
  运行、usage、token、费用和部署也均为 `0`。
