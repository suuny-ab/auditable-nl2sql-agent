# 当前开发状态

> 只保存当前事实；历史证据与回执见 [`docs/status-log/`](status-log/)，稳定边界见 [`PROJECT.md`](../PROJECT.md)。

| 字段 | 内容 |
| --- | --- |
| `state` | `in-progress` |
| 更新时间 | `2026-08-03` |
| 当前切片 | `VALUE-COLLECTION-041`：低基数字段有限取值采集与上下文注入 |
| 最近完成 | `NATIVE-METADATA-040`：PR #34 squash 为 `896546f`，main 三个 CI job 全绿 |
| 当前状态 | 候选 `69cd1c1` 已完成唯一复测：正确 `9/15`、成功 `1/7`，与基线持平；待推送 / Draft PR |
| 完成门 | 只读有限采集、第二库资源重建、主库不降与全门绿、唯一 15 题复测后合并 |
| 项目基线 | `origin/main@896546f99f49a4de7376f30ca7c8e1443080128f`；main CI run `30766117578` 成功 |
| 阻碍 | 无；本片唯一 15 次 Provider 调用已消费关闭，自动重试 0，不补跑、不部署 |

## 当前队列

- 采集器默认每字段 16 值、候选 32 字段、单值 256 字符、每字段 2 秒；高基数字段整字段跳过，
  不把前 N 个样本冒充完整枚举。
- 只在隔离治理构建中读取合成业务行；运行时加载版本化 JSON，不新增 workflow 工具或请求时扫描。
- 第二库目标资源为区域 / 分群 / 品类 / 状态 / 渠道 5 字段 17 值；主库 curated 上下文指纹保持不变。
- Python 全量 `136` 项本地测试、60 项定向、Web lint / build / SSR `3/3`、编译、44 包依赖、
  strict JSON、园丁 / 治理、Compose 与完整 non-root 容器合同均全绿。
- 唯一轮次 `values15-20260802T210307Z`：正确 `9/15 → 9/15`、成功 `1/7 → 1/7`、执行
  `7/7 → 7/7`、介入 `7/15 → 7/15`；usage `27426/1642/29068 → 29775/1446/31221`。
- 15 条均有 usage，transport 失败、自动重试、越权与全部非成功 SQL 执行均为 `0`；业务库
  SHA-256 前后同为 `2401b221…85b33`，报告 SHA-256 为 `da0e9ee5…7cea0f`。

## 下一检查点

- 提交复测证据口径，按夜班授权推送精确 head 并创建 Draft PR。
- 等 web / api / container 三路 CI 全绿后 squash 合并，复核 main CI；不部署。
