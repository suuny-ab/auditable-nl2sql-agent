# 当前开发状态

> 只保存当前事实；历史证据与回执见 [`docs/status-log/`](status-log/)，稳定边界见 [`PROJECT.md`](../PROJECT.md)。

| 字段 | 内容 |
| --- | --- |
| `state` | `in-review` |
| 更新时间 | `2026-08-02` |
| 当前切片 | `FROZEN-EVAL-30-027`：新增 10 条未见题并建立 30 题首份基线 |
| 最近完成 | `INTENT-ROUTING-FIX-026`：PR #20 合并为 `94a6715`，main 三个 CI job 全绿 |
| 当前状态 | 唯一 30 题评测完成：执行 `12/12`、正确 `26/30`、介入 `9/30`，待远端回执 |
| 完成门 | 推送精确 head、Draft PR 三个 CI job 全绿后 squash，并复核 main CI |
| 项目基线 | `origin/main@94a6715412f41a092464a3fd48990d916131b827`；main CI run `30752177107` 成功 |
| 阻碍 | 无；本轮真实 Provider 授权已消费，不再调用或补跑；远端流程已预授权 |

## 当前队列

- 扩充后首份基线为 `26/30`，与旧 20 题 `20/20` 分开保存；真实调用 `27`、total `32969` tokens。
- 新 10 题 `6/10`：6 条非成功路由全对；4 条成功均执行成功，但都发生非预期审批，其中 2 条列
  别名不符。越权与非成功执行均为 `0`，业务库哈希不变。
- 数据集 `30/30` 唯一、SHA-256 `66857af3…b0a6a`；Python 全量 `81` 项本地测试、Web `2/2` 及其余
  本地门全绿。

## 下一检查点

- 不再运行 Provider、评测器或改题；完成差异复核与脱敏文档提交。
- 按本单授权推送并创建 Draft PR；web / api / container 全绿后 squash，再复核 main CI。
