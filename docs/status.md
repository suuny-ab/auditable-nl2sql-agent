# 当前开发状态

> 只保存当前事实；历史证据与回执见 [`docs/status-log/`](status-log/)，稳定边界见 [`PROJECT.md`](../PROJECT.md)。

| 字段 | 内容 |
| --- | --- |
| `state` | `in-progress` |
| 更新时间 | `2026-08-02` |
| 当前切片 | `FROZEN-EVAL-40-030`：新增 10 条高难冻结案例并唯一运行 40 题 |
| 最近完成 | `UNSEEN-SUCCESS-FIX-029`：PR #23 合并为 `ee763129`，main 三个 CI job 全绿 |
| 当前状态 | 40 题合同与本地全门完成：定向 `5/5`、全量 89 项测试、Web `2/2` 均通过，尚未调用 Provider |
| 完成门 | 40 题合同与本地全门通过、唯一真实运行落盘、Draft PR 三路 CI 绿后 squash 并复核 main CI |
| 项目基线 | `origin/main@ee763129b259233b51e9c4944b597d963617a743`；main CI run `30754934078` 成功 |
| 阻碍 | 无；本轮最多 40 次 Provider、自动重试 0、推送与 Draft PR 已由夜班单预授权 |

## 当前队列

- 前 30 条规范化 SHA-256 固定为 `c229beea…be231a`，本轮不得修改。
- 新增 ID 固定为 `success-013..016`、`ambiguity-006..007`、`no_answer-006..007`、
  `unauthorized-005`、`injection-005`；最终类别为 `16/7/7/5/5`。
- 新成功题覆盖三表关联、标价 / 成交价、平均每单、`>=1000` 边界和组内 Top 1；非成功题覆盖
  近似歧义、缺字段、分析包装写操作和间接提示注入。
- 定向合同已证明 40 个唯一 ID / 问题、旧 30 题身份、四条成功结果和业务库哈希不变。
- 完整 40 题规范化 SHA-256 为 `c538bf96…97cef5`；知识层、生成逻辑与工作流相对 main 零差异。
- Python 全量 89 项测试、Web `2/2`、编译、44 包依赖、园丁、治理、Compose 与差异门全绿。

## 下一检查点

- 冻结候选提交；再核验题集运行副本、业务库、checkpoint 与 report 路径均满足唯一运行预检。
- 只运行一次完整 40 题，结果如实作为独立新基线落盘。
