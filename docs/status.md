# 当前开发状态

> 只保存当前事实；历史证据与回执见 [`docs/status-log/`](status-log/)，稳定边界见 [`PROJECT.md`](../PROJECT.md)。

| 字段 | 内容 |
| --- | --- |
| `state` | `in-progress` |
| 更新时间 | `2026-08-03` |
| 当前切片 | `SHOWCASE-VALIDATION-ARC-037`：展示页 v2 验证弧线、泛化三维与短板改道 |
| 最近完成 | `SCHEMA-SUMMARY-INJECTION-036`：PR #30 squash 为 `99ec46d`，main 三个 CI job 全绿 |
| 当前状态 | v2 页面、SSR 合同与 OG 卡片已完成且本地全绿；待远端评审，公网仍为 v1 |
| 完成门 | 每个数字双证据、边界声明和短板说明齐全；本地 / CI 全绿后 squash 合并，明确不部署 |
| 项目基线 | `origin/main@99ec46d0e27f87619f11cf0898a59e474a4de121`；main CI run `30761861494` 成功 |
| 阻碍 | 无；本片不调用 Provider、不运行评测、不部署 |

## 当前队列

- 调优弧线固定为同一 20 题 `14/20 → 17/20 → 20/20`，再到观察错误后修复的主库开发集
  `30/30 → 40/40`；五步不得写成连续未见泛化。
- 泛化三维固定为主库已见开发集 `40/40`、换 schema 总正确 `8/15`（成功题仍 `0/7`）、同义改述
  投影 `27/30`（只复跑三条）；每个数字链接版本化回执与对应 PR。
- 页面继续只读静态回放，不创建 run、不调用 Provider；API、评测资产、知识和运行时 diff 为零。
- Python 全量 `120` 项测试（`3.13.12`，`120/120`）、Web 生产构建 / SSR `3/3`、lint、编译、
  44 条锁依赖、strict JSON、园丁、治理、Compose config、凭据 / 反向导入和 diff 门全绿。
- 公网只读核验仍返回 v1 标题且不含验证弧线；本片 Provider 调用、评测运行和部署均为 `0`。

## 下一检查点

- 推送当前精确 head 并创建 Draft PR；api / web / container 全绿后 squash 合并并复核 main。
- 合并后归档本单；v2 部署等待用户另行批准。
